# Media, 2026-08-22 session

## What is here, and what it is NOT

| file | what it shows | sha256 |
|---|---|---|
| `mm_filmtool_chase_20260822.mp4` | 8 s, 240 frames, 30 fps, chase camera | `303291677d9eebdd…` |
| `mm_filmtool_front_20260822.mp4` | 8 s, 240 frames, 30 fps, fixed front camera | `91ba0754ebe4d405…` |
| `frames/*_f120.png` | frame 120 of each, for eyeball checking | — |
| `film_report_orbit.json` | ghost score, subframes, shot list | — |

**These are `film.py --drive orbit_walk`: the tool's own scripted joint swing.** They
prove the film pipeline — convergence, chase/fixed framing, ghost gate — on this box.
**They are not a capability demo and must not be captioned as walking.** The G1 does not
walk in them; it is posed through a scripted sweep.

The real-motion clip MM8 asks for is **not here**: `--drive policy` crashes in
`libomni.syntheticdata` + `libomni.graph.core`, the same subsystem as C-23
(`docs/twin/evidence/C23/C23_v2.md`), with 0 frames written.

## Ghost gate

    ghost score 0.00095 (max 0.01) PASS

## A warning about that gate, learned the hard way today

**The first clip of this session PASSED the ghost gate at 0.00095 and was unusable.**
`scene()` dropped the robot reference at the origin, so its legs were under the ground
plane: 240 frames of a torso and head sliding across a grid with nothing below the
pelvis. The gate measures temporal trails. It has no opinion about whether the subject is
intact, framed, lit, or even present.

Fixed (`FILM_SPAWN_Z`, default 0.80, from the same source the twin spawns at) and
re-shot; `frames/*_f120.png` are in this directory so the next person can check in one
look rather than trusting the number.

**MM8's acceptance criterion — "ghosting test green on every clip" — is necessary and
not sufficient.** Every clip needs a rendered frame looked at before it counts.

---

# THE REAL-MOTION CLIP — omni policy, 6.16 m walk. **Measured, not eyeballed.**

| file | sha256 |
|---|---|
| `mm_omni_walk_chase_20260822.mp4` | `1caf7a8895159f66…` |
| `mm_omni_walk_front_20260822.mp4` | `6fce8feb56d59166…` |
| `mm_omni_walk_20260822_pose.txt` | the base pose every 0.5 s, straight from the shoot's own log |
| `frames/mm_omni_walk_*.png` | stills for eyeball checking |

16 s, 480 frames, 30 fps, chase + fixed front, `--drive policy` on the real g1_omni
checkpoint. Commanded `0 m/s (2 s) → 0.5 m/s (12 s) → 0 m/s (2 s)`.

    t= 0.00s  pos=(-0.000, 0.000, 0.799)
    t=15.50s  pos=( 6.159, 0.243, 0.789)

**6.159 m travelled against 6.0 m commanded — 2.7 % over.** Base height held
0.763–0.799 m the whole way; lateral drift 0.243 m; never fell.

This is the capability clip MM8 asks for, and it is the **only** clip here that shows the
robot actually doing something. `film.py` already reported the pose for exactly this
reason — its own comment says *"'it looks like it is walking' is exactly the judgement
this campaign does not accept"* — which is why the number above is measured rather than
asserted from the stills. The two front stills are nearly indistinguishable by eye, and
would not have settled it either way.

## Why it works now, and why that is NOT C-23

`--drive policy` had been crashing in `libomni.syntheticdata`, which looked like C-23 and
is not. `run.py` guards its own `SimulationApp` behind **`FEROX_REUSE_KIT_APP=1`**, a
switch added specifically so `film.py` could import it; without it the import builds a
second app and the process aborts. Setting it, plus importing `run.py` before any render
product exists, produces the clip.

`_policy_drive`'s "INCOMPLETE — the G1 is FLUNG" docstring is therefore **stale**: the
three bugs it lists were already fixed, and what remained was this environment variable.

**C-23 is untouched by this** and stays parked under `C23_v2.md`. The two failures merely
produced similar-looking stacks.
