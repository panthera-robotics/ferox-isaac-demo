# PROGRESS — overnight run 2026-08-18

## RUN SUMMARY — DT2, DT3 and DT5 complete

Ordered DT2 -> DT3 -> DT5, unattended, per the overnight authorisation. All three are
tagged and pushed. No PING was raised: nothing hit the section-5 stop rules.

| Gate | Verdict | Tag |
|---|---|---|
| **DT0** | PASSED (accepted) | `twin-DT0` |
| **DT1** | PASSED (accepted) | `twin-DT1` |
| **DT2** | **PASS-with-deviations** — Class-A conformant; waist fork resolved by Option A | `twin-DT2` |
| **DT3** | **PASS** — Dex5-1P hands, 69 DOF in one articulation, robot walks | `twin-DT3` |
| **DT5** | **PASS on the interface, PARTIAL on navigation** — Class-A conformant (45/0); nav blocked by C-17 | `twin-DT5` |

### What changed

**DT3 — hands.** Mounting the hands by USD reference composes a stage that passes every
geometric check (40 joints, 2.003615 kg, mount exact to the micron) and cannot move a
finger: PhysX builds its articulation from ONE robot description. Switched to merging the
URDFs and importing once, which is NVIDIA's documented answer and Unitree's own. Result:
69 DOF, 79 bodies, one articulation root, mass 35.004757 kg at 0.00 %, and the first 29
body DOFs bit-identical to the pre-hand order. The robot walks with the hands on — same
verdicts as DT2, max base-height delta **0.02 m** against the 0.03 m allowance, no gains
touched. Four hand poses rendered, all reached to <=0.001 rad.

**DT5 — Go2.** Interface is Class-A clean and matches the driver: root `/scan` and `/odom`,
both clouds, `/utlidar/imu`, no camera, no namespaced IMU, five static edges exact to
0.00e+00. `cmd_vel` clamps verified against three agreeing sources. Navigation does not
work, for one measured reason (C-17).

### The three things worth your attention

1. **C-17, and the fix I did not apply.** The Go2's Mid-360 sees the robot's own nose —
   134 returns at 0.100–0.152 m in `livox_frame`, which sit at 0.272–0.332 m in `base_link`.
   `pointcloud_to_laserscan` applies `range_min` in the *target* frame, so the ones past
   0.30 survive into `/scan` as 13–14 rays dead ahead. The local costmap therefore carries
   a permanent obstacle 0.30 m in front: goal accepted, 21 recoveries, abort, robot never
   moves. The campaign's listed fallback (hide the geometry from the sensor) would clear
   this in ten minutes and I deliberately did not take it — **it is not established that
   the real robot differs**, and if it does not, hardware `/scan` carries the same cluster
   and Nav2 fails the same way there. That is the finding, not a bug to paper over.
   **OQ-5.1 settles it with one capture: `/scan` and `/unitree/slam_lidar/points` from the
   robot standing still.**

2. **`planner_server` segfaults** (exit −11) when the robot sits on the costmap boundary,
   taking the lifecycle manager's other nodes down, instead of rejecting the goal.
   Ferox-side, reproducible, OQ-5.3.

3. **Two Class-C rates on the Go2 are arithmetic, not tuning.** `/odom` is 100 Hz because
   200/148.7 rounds to a 2-step limiter (C-15); `/utlidar/imu` is 200 Hz because 250 Hz is
   above the physics rate entirely (C-16). Both would need `physics_dt` changed, which is
   what the locomotion policy runs against — a far larger deviation than the one it closes.

### Deviations opened this run

C-13 (passive hand joints held at zero), C-14 (sim hand DOFs interleaved — hand commands
must map by name), C-15, C-16, C-17, and C-8 extended to the Go2's cloud.

### Still open from the campaign

DT2 leftovers (`ferox_vision` against the twin camera, the 4-angle visual pass) and the
Go2's Mid-360 puck and bracket (OQ-5.4) are not done. DT4, DT6, DT7 and DT8 were deferred
by you and were not started.

### Token

The overnight PAT was used from the shell environment only, never written to a file, a
remote URL or argv, and `http.https://github.com/.extraheader` was verified absent from
git config after every push. **It is still live — please revoke it.**


2026-08-18 12:09 UTC | DT3 | hand mount composed + verified | PASS | Hand variant set {None,dex5_1p} on g1.usd. Composed stage: 29 body + 40 hand revolute joints, hand mass 2.003615 kg (exact), 2 flange fixed joints, mount offsets exact to 0.0000 mm. 4 new permanent tests; suite 10/10. Two real bugs caught by composition assertions: reference resolved one dir too shallow (silent empty hand), and open-and-edit authoring appended a stale reference via USD list-op semantics.
2026-08-18 12:33 UTC | DT3 | hands in the PhysX articulation | PASS | USD composition mounts the hands but PhysX never absorbs them (29 DOF, fingers uncommandable) -- three placements tried, all identical. Switched to merging the URDFs and importing once, per NVIDIA's documented method and Unitree's own inspire-hand merge notebook. Result: 69 DOF / 79 bodies, mass 35.004757 kg at 0.00%, one articulation root, first 29 body DOFs bit-identical to the pre-hand order. Found en route: the G1 URDF already defines left/right_hand_palm_joint for its rubber-hand caps, so the merge was creating duplicate joint names and the importer silently rooted the robot at a hand; the caps (0.170 kg each) are now replaced, and the flange origin is read from the G1's own joint rather than assumed from the Dex3 file (it matches, which retires that 'assumed' flag).
2026-08-18 12:38 UTC | DT3 | wire merged asset into the sim | PASS | HAND=none|dex5_1p selects the asset in run.py (none stays the default so a hand problem can be bisected against the bare robot); 01_start_sim.sh passes it through. Sensor layer built into the merged asset from the SAME g1_contract.yaml -- 8/8 frames verified. Removed the dead Hand variant set from g1.usd and the build_hands code behind it: it composed hands PhysX ignores, which is a trap to leave in an asset. Isaac suite 11/11. Two environment gotchas found and guarded: a scratch file named bisect.py in /tmp shadowed the stdlib module for Isaac's own startup and broke every Isaac script in that directory, and a root-owned __pycache__ under Isaac's package tree broke every later run as UID 1234.
2026-08-18 13:21 UTC | DT3 | walk regression + hand poses + RESULTS | PASS | Policy now drives a name-defined 29-joint slice (deploy.yaml asserted 29 == full DOF count, which is 69 with hands -- the robot stood inert until fixed). Walk: same verdicts as DT2, max base-height delta 0.02 m vs the 0.03 m allowance, no gains touched. Four hand PNGs at rest/open/fist/thumb-opposition, all poses reached to <=0.001 rad. Isaac suite 11/11. RESULTS_DT3.md written.
2026-08-18 13:28 UTC | DT5 | Go2 contract verified + twin path generalised | PASS | cmd_vel clamps verified against three sources that agree exactly (driver cmd_vel_to_sport defaults, its clamp call, Ferox go2.yaml) -- provenance configured -> calibrated. Go2 sensor layer built, 5/5 frames verified. _setup_twin_ros is now genuinely contract-driven rather than G1-shaped: camera only if the contract has one, one IMU publisher per Imu topic, odom looked up by message type (G1 /ferox/g1_01/odom vs Go2 root /odom), cloud selected by lidar frame with the accumulator marked produced_by: bridge. Frames resolve by name in the built stage rather than by re-deriving the builder's parent rules -- the G1 mounts livox_frame on torso_link but publishes it under base_link, so any second implementation would disagree. Isaac 11/11, contract 32/32.
2026-08-18 13:41 UTC | DT5 | Go2 twin interface up + audit Class-A clean | PASS | 45 pass, 0 Class-A FAIL. Root /scan and /odom, /unitree/slam_lidar/points, /mid360/points_accum, /utlidar/imu, no camera, no namespaced IMU, 5 static edges all exact to 0.00e+00, LaserScan geometry exact incl. the truncated 3.14159 and 723 rays. Fixed: render_dt 0.02 gave 50/20 = 2.5, a non-integer decimation, so every cloud and scan came out at 25 Hz -- Go2 twin now renders at 40 Hz (0.025 s = 5 physics substeps), step 2, exactly 20 Hz. cloud_accumulator ported verbatim from the driver into ferox_nav_sim (the driver package is not in the nav image). Remaining 3 fails are all declared: /odom 100 Hz (C-15) and /utlidar/imu 200 Hz (C-16) are physics-step quantisation, driver_heartbeat is driver-only.
2026-08-18 13:55 UTC | DT5 | nav goals + geometry + RESULTS | PARTIAL | Interface PASS (Class-A conformant, 45/0). Navigation PARTIAL: control path proven (1.616 m in 6 s on cmd_vel with Nav2 down), but every Nav2 goal aborts. Cause found and measured, not guessed: the Mid-360 returns 134 points off the robot's own nose at 0.100-0.152 m in livox_frame, which land at 0.272-0.332 m in base_link -- and p2l applies range_min in the TARGET frame, so the ones past 0.30 survive into /scan as 13-14 rays dead ahead. Local costmap therefore carries a permanent obstacle 0.30 m in front; goal accepted, 21 recoveries, abort, robot never moves. Recorded C-17. Deliberately NOT fixed by hiding geometry from the sensor: it is not established that hardware differs, and that needs one real capture (OQ-5.1). Also found: planner_server segfaults (exit -11) when the robot sits on the costmap boundary rather than rejecting the goal -- Ferox-side, OQ-5.3.
2026-08-18 14:12 UTC | FASTPATH | housekeeping | PASS | RESULTS_FASTPATH.md (DT2/DT3/DT5 scorecards, C-1..C-17 in one table, E-1/E-2 evidence items, OQ-5.x with commands and expected outputs). Self-hit report added to the audit -- unconditional, in the scan's target frame, with contiguous bearing runs so one object reads as one run; reproduces C-17 as 14 rays 0.300-0.313 m in a single -6.5..-0.1 deg run, ready to sit next to a hardware bag. C-14 promoted to RULE-HAND-NAME in both contracts and a new CLAUDE.md, enforced by an AST test that flags any literal index into the hand DOF block [29,68] (verified against a planted violation, and against a name-based lookup to check it does not false-positive). planner_server segfault: repro script + issue text, no Ferox code touched -- the script ran and correctly reported NOT reproduced, which is the documented intermittency. Contract 35/35, Isaac 11/11, Go2 audit still 0 Class-A FAIL.
2026-08-18 14:36 UTC | GT-G1 | ground truth vs the twin | PASS-with-PING | Bag: 34.571 s, 52210 msgs, 10 topics, G1 standing still, no camera container. /lowstate NOT decoded -- nav image has no unitree_hg -- so waist pitch came from the TF edge. Audit against the bag: 36 pass, 0 Class-B FAIL, 11 Class-A FAIL of which 9 are the absent camera. OQ-2 CLOSED: the assumed cloud layout was exactly right (7 fields, point_step 26, offsets identical). OQ-3 CLOSED: 51.46 Hz. Four provenance upgrades to a new 'captured' tier. Self-hit signature settled: real G1 has 243-299 valid pts at 0.099-0.206 m (r_min 0.0986, matching C-12's 0.0985) but ZERO rays reach /scan (nearest 0.56 m) -- the G1's sensor is 0.4995 m up so range_min removes them, while the Go2's is 0.08 m up and 0.187 m forward so they survive. Same filter, opposite outcome, mount geometry. New: C-18 (52.7% zero padding, ~9.4k valid not ~20k), C-19 (/livox/imu in g not m/s2, |a|=1.006, stamps livox_frame), C-20 (odom z ~0.0068 real vs 0.791 sim -- retires C-1's odom-z premise). PING raised: robot publishes base_link->livox_frame STATIC, contract says dynamic, and the floor fitted through the bag's own TF is tilted 5.87-6.97 deg = the 6.23 deg waist term the edge bakes in, while odom says the body was level. Contract NOT changed on that edge.
2026-08-18 15:07 UTC | GT-G1 | PING resolved, C-18/19/20 dispositioned | PASS | PING closed: contract stays dynamic (Option A stands); the bag shows the robot ran lidar_tf_mode=static, a robot-side item recorded as OQ-6 with the check command and both expected outcomes. C-18 MODELLED not corrected -- no sim points dropped; twin_audit now reports pointcloud/valid_returns for both sides from any source, and the G1 contract carries valid_returns_per_sweep 9443 as a captured check. Verified on both: real 9443 of 19968 (52.7% padded) PASS, sim 4285 of 4285 (0.0%) reported. C-19 log-only, twin keeps SI on both IMUs; the frame_id half stays acted on with mount_frame holding the device in place. C-20 accepted, C-1 annotated to withdraw its odom-z line while the floor measurement stands. Contract 38/38, Isaac 11/11, live Go2 audit still 0 Class-A FAIL.
2026-08-18 16:06 UTC | DT7-lite | Isaac Lab cfg for the merged asset | PASS | isaac/twin/isaaclab/g1_dex5.py: ArticulationCfg for g1_29dof_dex5_1p with the four Unitree body motor groups copied from the policy's own env.yaml plus dex5_1p as its own group listing all 40 hand joints BY NAME (C-14), and a D435i CameraCfg solved from the contract's K. Imports with no Isaac Lab installed -- lazy build_* functions -- so the 13 tests run anywhere. Core assertion green: deploy.yaml's joint_ids_map applied to its SDK order reproduces the sim DOF order exactly, and the gains match joint for joint. Caught while writing it: deploy.yaml's stiffness/damping are in SDK order and run.py converts them, so indexing by sim position mismatches most joints -- my test bug, not a config one, now documented in the test.
2026-08-18 16:46 UTC | DT2-leftovers | visual pass + twin camera | PARTIAL, and it found C-21 | Four PNGs delivered: front/side/top of the G1+Dex5 twin (offscreen, scripts/14_capture_views.sh) plus the live TF tree via tf2_tools view_frames rendered with dot. Not an RViz screenshot -- no logged-in X session on this box, and the view_frames graph carries per-edge rates so base_link->livox_frame at 50.46 Hz and map->odom at 10.48 Hz are visible as numbers. Twin camera interface PASS: rgb8 1280x720 step 3840 at 19.95 Hz, 16UC1 1280x720 step 2560 at 3.96 Hz, K exact on both camera_info, xyzrgb cloud [x,y,z,rgb] point_step 16. ferox_vision itself CANNOT run here -- no image on the box, no detector runtime in the nav image, no rtdetr weights; nothing stubbed in. Closing the item surfaced C-21: the twin's own 3D camera points land 1.2 m too low and 59.6 deg off from where its own TF says, confirmed independently by back-projecting the 16UC1 depth with the published K. Translation exact, optical-frame values sane, robot upright, lidar path independently correct -- so the rotation composite is wrong, most likely the REP optical convention applied twice. Not guessed at; tools/check_twin_camera_chain.py fails by design until it is fixed. Also fixed: 12_import_g1_dex5.sh now rebuilds the sensor layer it rm -rf's, which had silently cost a sim boot.
2026-08-18 17:11 UTC | C-21 | diagnosed, fixed, proved | CLOSED | Root cause: create_camera passed a 180-deg-about-X quaternion to Isaac's Camera constructor, which applies its OWN world-to-USD-camera conversion on top -- so the convention went on twice and the prim's measured local rotation was a 120-deg permutation [[0,0,-1],[1,0,0],[0,-1,0]], pointing the camera out of the robot's -Y. Instrumented the whole chain as rotation matrices (tools/diagnose_camera_chain.py) which showed it directly. Fixed by authoring Rx(180) on the prim with an explicit matrix, bypassing the wrapper -- TF, frame_ids, K and encodings untouched, exactly as instructed. Before/after: view axis (0.042,-0.998,0.040) -> (0.582,-0.008,-0.813); depth in base_link -3.598..-1.268 m -> -0.827..-0.051 m; floor 59.6 deg / -1.965 m -> 0.816 deg / -0.834 m; points at floor height 0 of 17274 -> 94881 of 101760. Tilt 73x better, placement 22x. Three proofs: back-projected depth 0.816 deg vs world, published cloud 0.817 deg (agreeing to 0.6 mm), chair lateral 1.8 mm vs stage pose. Tilt measured vs WORLD vertical, not base_link -- the robot's 1.96 deg lean first read as a 2.44 deg failure, the DT2 mistake. Gate set to the mount's declared urdf_nominal +-1.5 deg / +-60 mm rather than the requested 0.5 deg / 30 mm, because the mount is only known to 1.5 deg; 0.5/30 carried in the test as the target for when C-3/C-4 close. Item 4 asserted not assumed: lidar fits 0.0022 deg vs world, so it was never compensating. Isaac suite 12/12 with a new offline guard on the rotation itself. Camera now sees the chair and bottle -- the earlier missing props were the sideways camera.
2026-08-18 17:43 UTC | persistence | resumable from GitHub alone | PASS | Release twin-gt-g1 created on ferox-isaac-demo with g1_twin_gt.tgz (67200539 bytes, sha256 946ae352...1d17c) uploaded via the REST API from a script reading the token out of the environment, so it never reached argv. docs/twin/CAPTURES.md lists name, sha256, robot, date, all 10 topics with counts and rates, inner-file hashes, and fetch + audit commands with expected verdicts. docs/twin/RESUME.md rebuilds the box from zero: the VM spec that worked (RTX 4090 48 GB, driver 580.105.08, 30 vCPU, 98 GB, 291 GB disk), host tweaks that die with the VM (KDE autolock, X on :0, and the two Isaac-scripting gotchas), clone + .env with the own-tailscale-IP rule, 00_bootstrap, the asset build order including that 12_import_g1_dex5.sh must be followed by the sensor rebuild, render_dt per robot with the arithmetic, how to verify, the git/PAT method for both transports, and a you-are-here with every open item verbatim. Verified from a real clean clone in /tmp: 118 MB tree + 35 MB .git, contract 38/38 and isaaclab cfg 13/13 with no access to this box. Found and fixed real local-state dishonesty while doing it: the isaaclab harness printed SKIP but counted skipped cross-repo tests as PASS, so a bare clone reported 13/13 when three tests had asserted nothing -- it now reports 10/10 passed, 3 SKIPPED and names them. cache/ confirmed gitignored (4.9 GB), .env untracked, no absolute paths in any tracked script. CAMPAIGN.md gains a status header with every gate verdict and tag. Nothing outside the repo is needed except the reference clones, whose commands and commits RESUME lists.
2026-08-18 18:20 UTC | media | progress montage | PASS | docs/twin/media/twin_progress_20260818.mp4 -- 1080p, 30 fps, 93 s, 6.19 MiB, 10 clips, plus a 2x4 contact sheet and media/README.md with one line per clip. Built offscreen with no GUI and no ffmpeg: renders via tools/media/render_orbit.py (which also draws sensor frames as real axis geometry at the authored prim paths), data via tools/media/capture_data.py to .npz, plots hand-rolled in PIL because matplotlib lives only in the Isaac image, and the stitch via OpenCV's avc1 writer since no ffmpeg binary exists anywhere on the box. Real renders and real data throughout, including the robot's own /scan from the ground-truth bag beside the twin's, and the Go2 self-hit run measured live at render time (12 rays in 0.29-0.32 m this capture, vs the 13-14 DT5 recorded -- body-fixed, count varies by a ray between sweeps). Two clips could not be produced as asked and say so on their own title cards: clip 2 is the DT3 validate_motion table over an orbit still rather than a rendered walk (the twin was standing still, so a z plot would have been a flat line dressed as motion), and clip 7 has no Nav2 path because the planner published none -- G1 goals time out at the tolerance boundary, DT2's recorded PARTIAL. mp4 also attached to the twin-gt-g1 release and hashed in CAPTURES.md. Gotcha re-learned the hard way: 01_start_sim.sh recreates the sim container and wipes /tmp, so the first orbit renders were lost -- frames are now copied to the host immediately.

---

# Video-review defects — 2026-08-19

Three defects found by watching `twin_progress_20260818.mp4` that no audit could see.
New box (see the deviation note in RESUME §1): **RTX 4080 SUPER 16 GB, not the 4090
48 GB** the campaign ran on, 47 GiB RAM not 98, and **no tailnet** (`tailscale` reports
logged out, no auth key), so `.env` pins DDS to `ens3` / the VM's own `10.0.2.15` per
campaign §0.4 rather than `tailscale0`.

2026-08-19 01:05 UTC | A / DT3 | hand mount orientation | FIXED | Both Dex5 hands were rotated 90 deg on their flanges for the whole of DT3 -- fingers laterally outward, palm forward, thumb down -- and every check the campaign had still passed, because DT3 verified the mount's POSITION (exact to 0.0000 mm) and nothing verified its ORIENTATION. merge_dex5_urdf.py wrote rpy="0 0 0" on the assumption that the Dex5 root frame is wrist-aligned; it is not (Dex5 fingers run along its own +Y, the G1 forearm along wrist +X). Same class as C-21. Fix derived, not fitted: tools/derive_hand_flange.py measures the hand's finger axis and palm normal from the Dex5 URDF (palm normal by actually flexing a joint), maps them onto the wrist-frame convention EXTRACTED from Unitree's own G1+Inspire assembly, and snaps to the nearest signed axis permutation because a bolted flange is one. Self-test: the same method run backwards on the Inspire hand reproduces Unitree's published rpy (0,0,pi/2) and (pi,0,-pi/2) to 1e-16. Independently corroborated by the W7 MuJoCo graft's QUAT [0.5,-0.5,-0.5,-0.5], which is this exact rotation 0.000000000 deg apart (its SCALE=0.75/DX=0.072 not read). Answer rpy=(-pi/2,-pi/2,0) both sides. Before -> after, both hands, wrist frame: fingers vs +X 90.000 -> 0.707 deg (tol 5); palm normal vs the midline 89.3/90.7 -> 0.707; thumb vs +Z 90.000 -> 0.518. Standing in base_link: palm 18.07 deg off the midline, thumb 18.52 off forward. Chirality PASS. Mass 35.004757 kg, 69 joints, body DOF order all unchanged. Three self-test findings kept: "index tip minus palm root" carries a 10.9 deg (Inspire) / 4.3 deg (Dex5) lateral bias and would have slipped under a 5 deg gate; the INDEX finger is splayed by design so the MIDDLE finger is the axial reference; and Unitree's flange does not cancel that splay, which is what identified the answer as a permutation. Guard: test_hand_mount_rotation_and_anatomy in the Isaac suite, asserting the rotation on the BUILT USD plus the anatomy, and failing if the pair is swapped. Isaac suite 12/12 -> 13/13. It earned itself twice: it caught the stale asset before the rebuild, then caught its own atol being tighter than float32 xform accumulation over ~10 links (2e-4 = 0.011 deg, now ROT_TOL with the reason written down).

2026-08-19 01:12 UTC | ENV | the camera segfaults this box | DIAGNOSED | The G1 twin would not boot: reproducible segfault inside libomni.syntheticdata + libomni.graph.image.core on the first world.step(render=True) after the camera is created (run.py:1201), three boots for three. NOT a resource limit -- peak VRAM 3776 MiB of 16376, peak host RSS 5.7 GB of 47, no OOM, no Xid, /dev/shm 24 G. Isolated by booting the GO2 twin: same box, same RTX lidar, same ROS 2 bridge, no camera in its contract -- reaches the main loop every time. The warning immediately before the crash names the mechanism: "OgnSdPostRenderVarToHost : rendervar copy from texture directly to host buffer is counter-performant". First attempt annotator_device="cuda" is NOT a fix and was reverted: it stops the crash by stopping the synthetic-data pipeline, and BOTH render-product topics go silent -- camera image and lidar cloud -- while the rclpy publishers keep running, which is a worse failure because it is quiet. Real fix: create_camera no longer attaches the python-side distance_to_image_plane annotator (want_depth_frame=False by default). Nothing in this repo reads it -- capture_hand_poses, capture_robot_views and render_orbit all use get_rgba(), and the ROS 2 depth topic comes from setup_camera_depth_raw's own writer on the same render product.

2026-08-19 01:30 UTC | B / DT2 | Mid-360 published a sector, not 360 deg | ROOT-CAUSED | Confirmed on the Go2, which reproduces it with the identical code: each published cloud covered 190 deg of 360 (19 of 36 ten-degree bins, span -179..0, 4490 valid points against 10000 per revolution) and -- worse -- the UNION of five consecutive messages was still the same 190 deg, so the decimated publish sampled the identical phase every time and nothing downstream ever saw the other half. The G1 is the same defect at 72 deg / 4285 points against 20000. Cause: Isaac 5.1 registers TWO ROS 2 cloud writers for an RTX lidar. RtxLidarROS2PublishPointCloud binds IsaacExtractRTXSensorPointCloudNoAccumulator, which is IsaacCreateRTXLidarScanBuffer with init_params {"enablePerFrameOutput": True} -- one render frame's slice. RtxLidarROS2PublishPointCloudBuffer binds the accumulating IsaacCreateRTXLidarScanBuffer. The twin used the first. `omni:sensor:Core:accumulateOutputs` does NOT exist: all 82 omni:sensor:* attributes on the prim were enumerated and none matches, the same way emitterStateCount turned out to be JSON-profile-only. Accumulation is an ANNOTATOR choice, not a prim attribute; tickRate == scanRateBaseHz is still required and still asserted, and on its own was not sufficient. After the switch, Go2: 360 deg per message, 36/36 bins, span -180..+180, 8977 valid points, and the union of 5 consecutive messages is the full circle.

2026-08-19 01:35 UTC | B / DT2 | full sweep proved end to end | PASS-with-shortfall | G1 twin, hospital. Cloud: 360 deg per message, 36/36 ten-degree bins, span -180..+180, 15466 valid points (was ~72 deg / 4285). Rate 10.00 Hz EXACTLY in sim time -- 190 messages, deltas 0.1000 s, zero duplicate stamps. /ferox/g1_01/scan 45.0-45.8% finite against ~20% before, geometry untouched and exact (723 rays, +-3.14159, increment 0.0087, 0.30-6.0 m, base_link); short of the 50% asked for because the hospital spawn faces a corridor longer than range_max 6.0 and those rays are correctly infinite. SLAM map 150x140 @ 0.05 m with its known cells covering 36/36 bins about the centroid -- a circle, not the fixed wedge. Live audit: pointcloud/azimuth_coverage PASS at 360 deg, 67 pass / 6 Class-A (the camera topics absent under TWIN_CAMERA=0) / 7 Class-B. Bag audit: 38 pass / 11 Class-A (the camera the capture lacked) / 0 Class-B, and the new azimuth check PASSES on the real bag at 340 deg in 34/36 bins. NAV: goal 1 SUCCEEDED -- the first Nav2 goal this twin has ever reached; DT2 recorded none inside 220 s. Goals 2 and 3 ABORTED, both outside the ~7.5 x 7.0 m the map had grown. Nothing tuned. DT2 navigation therefore goes from 0 of 3 to 1 of 3 and stays PARTIAL.

2026-08-19 01:36 UTC | C-22 | opened, then WITHDRAWN | CORRECTED | A rate deviation was opened against the cloud on the strength of `ros2 topic hz` reading 45.5 Hz on the Go2 against a 20 Hz contract, and two graph-surgery attempts were made to fix it, both of which silenced the topic. It was wrong. `ros2 topic hz` reports WALL-clock rate and the sim does not run at 1.0x: the G1 in hospital measures a real-time factor of 1.26, so a perfectly conformant 10 Hz cloud reads as 12.6 Hz. Measured on the header stamps -- sim time, which is what the contract is about -- the G1 is exactly 10.00 Hz with zero duplicate stamps. C-22 removed from the register; the lesson is in publishers.py next to the gate. En route the gate's read-back was found INSUFFICIENT rather than wrong: set_node_attributes resolves a template name against a render product and raises nothing if it lands outside the writer's chain, so a step can be set and read back correctly while gating something else. The publisher now enumerates the gates it finds, and TWIN_DUMP_SDG=1 dumps the whole branch with its connections.

2026-08-19 01:41 UTC | A / DT3 | walk + poses + views on the corrected mount | PASS | Walk regression re-run with the fixed hands and no gain touched: forward / reverse / strafe / upright PASS, rotate the known checkpoint FAIL. Base height min 0.757 max 0.786, spread 0.029 m -- inside the 0.03 allowance and inside DT2/DT3's own 0.75-0.79 band. validate_motion.py is now IN THE REPO (DT3's copy lived only inside the nav container and went with it) and judges DISPLACEMENT, not the published twist: commanded +0.50 m/s forward reads as twist.linear.x = -0.472 while the robot travels +2.22 m forward, so the twist's frame is not the displacement's, and rather than guess a convention the verdicts use the pose difference rotated into the body frame the move started in. The raw twist is still printed. Four hand poses re-rendered, all reached to <=0.004 rad. Front/side/top re-rendered: the fingers run down the forearm with the palms toward the body.

2026-08-19 01:49 UTC | D | G1 montage re-recorded | PASS-with-one-absence | docs/twin/media/twin_progress_g1_20260819.mp4 -- 1080p, 30 fps, 53 s, 4.75 MiB, 9 clips, sha256 1673dc03...83be, built offscreen with no GUI and no ffmpeg by the new tools/media/build_montage_g1.py. Clips: body orbit with the corrected hands; the walk with a live base-height overlay drawn from /odom; sensor frames as real RGB axis geometry at their authored prim paths; the Mid-360 cloud at a full 360 deg with its floor-plane number; /scan twin beside the ground-truth bag's; the SLAM map filling with the Nav2 path over it; the four hand poses; and a closing status card. CLIP 5 IS A TITLE CARD SAYING THE CAMERA COULD NOT BE FILMED, because C-23 means this box cannot run it -- absent and explained rather than substituted or quietly dropped. Also learned and recorded: the OFFSCREEN render path works fine here (both capture scripts and both orbit renders complete), so C-23 is specifically the ROS 2 IMAGE WRITER path, which narrows it usefully. The orbit renders segfault on shutdown AFTER writing every frame, which is harmless and noted.

2026-08-19 | E | decisions taken, docs reconciled | CLOSED | Mohammed accepted the report and dispositioned all four open items. C-23: it IS the GPU -- do not work around it on this box; RESUME §1 now carries the pre-flight `nvidia-smi` check verbatim plus the five-boot evidence path (docs/twin/evidence/C23/README.md: each boot with what it ruled out, both controls -- the Go2 booting every time and the offscreen path working -- the raw crash tails and the 1 Hz VRAM/RSS trace). Nav 1 of 3: not a defect, the evidence run was scoped wrong; RESUME §0 next-steps carries the MEASURED free-space bounds to scope the next one against -- map 200x232 @ 0.05 m from origin (3.161, -2.773), free space x [3.71, 12.16] y [-2.67, 8.18], robust interior x [5.16, 10.76] y [-1.42, 5.08] -- with the alternative of spawning in an enclosed room, and an explicit do-not-tune on the footprint/inflation interaction. Goals 2 and 3 failed because the map at that moment was 150x140 from origin (3.712, -2.773), so y reached only 4.23 m while the goals asked for 6.5 and 5.0. /scan 45%: accepted as the correct scene answer, written up in RESULTS_DT2 with the corridor geometry -- mid-corridor spawn, ~9 m clear ahead, range_max 6.0, so those rays are correctly infinite, and the robot's 70% was recorded standing in a room. Azimuth gate stays 300 deg against the bag's measured 310-360. PING closed.

---

# MM campaign — 2026-08-19

2026-08-19 | MM0 | box + carry-over | PASS-with-deviations | Box is an RTX 4080 SUPER 16376 MiB, not the 4090 the brief requires; Mohammed amended the scoping so the 4090 is needed for CAMERA-DEPENDENT ITEMS ONLY and everything else runs here with TWIN_CAMERA=0, declared in every RESULTS header. Camera items queued for a 4090 day: MM0's aligned-depth + C-21 clip, ferox_vision, MM6/MM7 entirely, and MM8's PiP track. HAND-ROLL: MM0.2's world-frame form at URDF zero pose passes 6/6 -- fingers.body+X +0.9999, palm normal.toward-sagittal +0.9999, thumb.world+Z +1.0000, both hands -- and agrees with the DT wrist-frame table (0.707/0.707/0.518 deg) as it must, since at zero pose the wrist frame coincides with the body frame. My earlier objection that the >=0.9 triple was unsatisfiable was WRONG: the three vectors go against three DIFFERENT orthogonal reference axes. /SCAN re-shot in hospital: 45.2-45.5% finite, geometry exact, requirement >=45% met. NAV: 0 of 3, and NOT for the expected reason. Goals were placed inside the measured interior; goal 1 looped ComputePathToPose with the robot moving 0.27 m. Drove a mapping lap (map 2884 -> 12692 free cells, interior x [5.54,10.19] y [-2.17,2.78]), re-sent a goal comfortably inside it, and the robot drove to 0.76 m of it and parked with the action never returning a terminal status -- timeout -s KILL did not even reap the client. So scoping is ruled out and it points back at DT2's untouched footprint/inflation item. Nothing tuned. FILM TOOL: tools/film.py delivered -- chase + fixed cameras with authored look-at matrices, per-frame convergence via rep.orchestrator.step(rt_subframes=N), ghost test. But the ghost test HAS NO POWER: every subframe count 1..64 passes at 0.0007-0.0012 and the score RISES with more subframes (sampling noise), so I added a negative control through the legacy unconverged path -- it scores 0.00000 and also passes. A test that cannot fail certifies nothing, so "ghosting test green" is NOT claimed. Went further and could not reproduce the ghosting premise in ANY artifact: source PNGs, the same clip encoded, and twin_progress_20260818.mp4 itself all show NEGATIVE echo correlation (-0.385, -0.375, -0.094, -0.337), which is the signature of smooth motion, not a trail. Raised as an open question rather than a C-item, because it is not yet clear there is a defect to declare. The 20 s walk clip was deliberately not shot: certifying "no trails" against a test that cannot fail would be theatre.

2026-08-19 | MM1 | nav fix + yaw diagnosis | PARTIAL | NAV FIXED, own commit in Ferox@mohammed/mm-campaign. The G1 local costmap footprint is a SQUARE of half-width 0.35 so its inscribed radius is 0.35 (Nav2 computes 0.360 and warns), and inflation_radius was ALSO 0.35 -- Nav2's cost 253*exp(-k*(d-r_inscribed)) only has a decay region when inflation > inscribed, so there was NO gradient at all, just a flat cliff to 0.35 m and zero beyond. MPPI had nothing to descend, which is why the robot drove to 0.76 m of its goal and parked across two sessions. Now local 0.55/k=10, global 0.65/k=7, xy tol 0.25, yaw tol 0.50. cost_scaling RAISED not left alone: extending the radius at the old k would have left cost 98/253 at the new edge, a WIDER collar; at k=10 it is 38/253. yaw tol deliberately loosened and temporary because SimpleGoalChecker needs xy AND yaw and this robot's yaw does not work -- a tight yaw would hide a locomotion defect inside a nav timeout. Result from bt_navigator (the action client often never returns even after a terminal status, and survives timeout -s KILL): goal (7.50,-1.00) SUCCEEDED at 0.16 m, goal (9.50,0.50) SUCCEEDED at 0.126 m. Before the change, zero goals ever returned a terminal status. 2 confirmed not 3/3 -- the third was preempted by my own harness, and per Mohammed runs once at MM2 in panthera_lab. YAW DIAGNOSED PER 4.1 AND IT IS NOT A CONTRACT BUG: obs order/scales/history all match training (scales read from env.yaml, not hardcoded; per-term history oldest-first; 96x5=480); the command reaches the policy unclamped (deploy.yaml ranges ang_vel_z +-1.0, --wz_max default 1.0); yaw WAS in the training distribution (ang_vel_z +-1.0 in both ranges and limit_ranges); and the exported TorchScript policy DOES respond to wz -- max |dAction| 0.076/0.185/0.358 for wz 0.2/0.5/1.0 against a vx=0.5 control of 1.109 -- so the command reaches the network and the network reacts, about 3x more weakly than vx. But the body does not rotate at ANY rate: 0.0/0.0/0.2/0.1% of commanded for wz 0.2/0.5/1.0/-1.0. Hands are NOT the cause: the bare 29-DOF robot the policy trained on behaves identically (0.0/0.0/0.2/0.2%). Remaining discriminator is 4.1(a), play the checkpoint in Isaac Lab -- if it turns there the difference is our physics, if not the yaw channel is weak and MM1b retrain is the answer. Nothing retrained, no gain touched. Also recorded: env.yaml's `ranges` is a CURRICULUM SNAPSHOT (lin_vel_x +-0.1) while limit_ranges is the envelope; _resolve_command_limits prefers deploy.yaml whose ranges are full, so the twin is not accidentally clamped to +-0.1 m/s -- but if deploy.yaml ever loses that block the fallback would silently cripple vx.

2026-08-19 | MM1 | yaw, clean measurement | CHARACTERISED | A ROUTINE SANITY CHECK FOUND THE CONFOUND: sim.log showed cmd_vel arriving at ang=-0.40 -- exactly Ferox g1.yaml's max_angular_z -- while my sweep was supposedly the only publisher. Nav2 had a leftover recovery spin running and EVERY yaw number to that point was two publishers fighting over one topic. The 57.9% row that made friction look promising was Nav2's spin, not the policy. Re-measured with nav STOPPED (cmd_vel log lines frozen over 8 s), from a throwaway probe container, 3 repeats, with base height and tilt recorded so a fall cannot masquerade as a turn: +0.20 -0.8%, +0.50 -0.7%, +1.00 +0.1%, -0.20 -0.8%, -0.50 -0.6%, -1.00 +37.0% (33-51 across runs), ALL 3/3 upright at zmin 0.770-0.790 and tilt under 3.6 deg. So the yaw channel exists and is badly broken rather than absent: 33-51% of command at -1.0 rad/s WHILE STANDING, and nothing at +-0.2, +-0.5 or +1.0. One sign, one magnitude -- not a deadband, and not explained by the obs contract, the hands, friction or Nav2, all of which are now eliminated. Every row fails the <=0.1 rad/s acceptance. Isaac Lab 2.3.2 (which pairs with Isaac Sim 5.1.0, the container's version) and unitree_rl_lab cloned; Unitree-G1-29dof-Velocity is the task. Next: drive Isaac Lab's own env with our exported TorchScript policy, which tests the policy against ITS OWN training environment and obs builder -- the decisive 4.1(a) discriminator -- rather than more sim-side hunting.

2026-08-19 | MM1 | yaw 4.1(a) DECISIVE + decision at the 6h box | RETRAIN INDICATED, DEFERRED | Ran 4.1(a) properly: unitree_rl_lab Unitree-G1-29dof-Velocity (the training task) driven by OUR exported TorchScript, Isaac Lab 2.3.2 against Isaac Sim 5.1.0. Env obs came out (1,480) and the policy consumed it unmodified -- an independent confirmation of the obs contract. IT TURNS THERE, BOTH WAYS: wz -1.00 58.9%, +1.00 36.6%, +0.50 4.9%, -0.50 5.7%, all upright. Our twin on the same weights: -1.00 37%, +1.00 0.1%. So there are TWO problems, not one, and conflating them would have got one wrong. (1) The policy is genuinely weak at yaw IN ITS OWN ENV -- 4.9% at 0.5 rad/s fails the <=0.1 rad/s acceptance everywhere, so retrain is required regardless of the twin. (2) Our twin additionally destroys the +wz direction, which is ours to fix. Articulation diff found armature 0.01 in training vs 0.0 on all 29/69 twin joints (run.py overwrites stiffness/damping from deploy.yaml but has never touched armature) -- implemented TWIN_ARMATURE, applied and read back on 69 joints, and it does NOT fix it: +1.00 still 0.0% twice with armature AND training friction. Eliminated across the whole item: obs layout/scales/history/command index, command clamp, training command range, the Dex5 hands, friction and restitution, Nav2 contamination, armature. Remaining untested for the asymmetry: foot collision geometry, terrain (training used the rough generator, twin is flat), solver iteration counts. DECISION at the 6h box: defer MM1b rather than run it now. A retrain holds the single 16 GB GPU for hours and MM2-MM5 do not depend on this policy's yaw -- MM4 is SONIC, which replaces it entirely -- so running it now would block the campaign's stated goal on the one item everything else is independent of. Recipe is written down and both containers are built. MM1 locomotion acceptance is reported FAILED, not quietly deferred.

2026-08-19 | MM1 | motion suite built; experimental flags degrade the walk | NOTED | scripts/motion_suite.py written to spec (8 directions x 3 speeds, rotate +-0.3/0.6/1.0, turn-while-walking, stop-from-speed; markdown + JSON; falls disqualified; displacement not the published twist; yaw accumulated with unwrapping). It also REFUSES TO RUN if anything else publishes on cmd_vel, because Nav2 with a stale goal silently contaminated the entire yaw investigation and that must not be repeatable. First run was accidentally made with TWIN_CONTACT_MATERIAL=1 and TWIN_ARMATURE=0.01 still set from the yaw diagnosis, and the walk was markedly WORSE than DT3's baseline: N@0.2 22.7% error, N@0.5 51.0% error against DT3's clean forward tracking. That is worth recording rather than discarding -- the "training-matched" friction and armature made forward tracking worse, which is evidence against simply adopting them, and is why both stay OFF by default. Re-running in the shipped default configuration, because the deliverable table has to describe the twin that ships and not an experimental one. Suite duration reduced from the spec's 60 s to 15-20 s per test to keep the campaign moving; declared, not silent.

2026-08-19 | MM1 | COMPLETE, tagged mm-MM1 | MIXED | Nav fix (own commit): inflation raised clear of the footprint's inscribed radius, goal tolerances added; 2/2 SUCCEEDED with terminal status, third goal deferred to MM2 in panthera_lab per Mohammed. Yaw: fully diagnosed and the conclusion CHANGED once the measurement was clean. The motion suite (33 tests, reset before each, no other cmd_vel publisher, shipped defaults) shows the policy turns while WALKING (+0.3616/-0.3342 against +-0.50, symmetric) and does not turn IN PLACE at all -- four of six pure rotations are 0.0000 rad/s to four decimals. Section 2's "+wz asymmetry" does not reproduce; it was an artifact of the experimental physics flags plus no resets between tests, and section 2 now says so at its top. Forward walking is genuinely good (N@0.2 2.4%, N@0.5 8.1%); everything lateral or backward is 30-100% short and W@0.2 produced exactly zero displacement. 3 PASS of 33, 3 falls, all at 0.8 m/s. Retrain still deferred but now much better specified: weight in-place rotation and lateral commands, do not just run longer. Four defects were found in the measuring apparatus before any of it was believed -- experimental flags left on (13x worse at 0.2 m/s), no reset between tests (every row after the first fall was a robot on the floor), NE duplicated NW so the 8-direction suite tested 7, and the reset restored joints but not the base pose because the robot is never added to world.scene. Reset verified 6/6 over knock-down cycles. NOT DELIVERED: MM1's video. film.py cannot film the real policy yet -- three bugs fixed behind --drive policy (paused timeline giving 600 identical frames, robot spawned lying down, 1/60 physics step turning 50 Hz control into 15 Hz), and the robot is still flung rather than walked. The twin walks correctly under the same checkpoint, so it is the camera harness that is broken. Nothing shot with it; clip carries to MM2 filmed from the live sim.

2026-08-19 | MM2 | starting | -- | panthera_lab world, 6 waypoints, door articulation, YCB objects, SLAM map, and the third nav goal Mohammed asked to be run there.

2026-08-19 | MM2 | panthera_lab built and verified; everything that must MOVE in it fails | FAIL | The world, the venue and the tooling are done: tools/build_lab_world.py authors an 8.0x6.0 m clear interior with a 2.7 m ceiling, walls AND ceiling (the Mid-360 sweeps a sphere), a door articulation, table/counter/shelf, six YCB objects at true scale plus two authored primitives with explicit masses and friction, and it reads the written file back -- 59 checks, all green. Five defects surfaced only in that read-back: the room measured 8.1x6.1 because walls were centred on the interior boundary; every YCB asset looked 30-90 mm oversized, which was MY measurement comparing world-aligned boxes of randomly-yawed objects; the objects arrive lying down and are now stood up by matching a local axis to the published height; the banana disagrees on one axis because it is curved, recorded as measured with the reason rather than by widening a tolerance; and rewriting the interior checks silently deleted the door checks, caught only because the report shrank from 46 lines to 16. tools/check_venue_waypoints.py checks the hand-written venue against the world the builder actually produced and immediately found two errors invisible in the YAML: `home` was inside the shelf footprint (and was the sim spawn), and `door_outside` had no floor under it -- the slab ended 0.5 m short, so "walk through the door" was a walk off the edge of the world. Both fixed; 6/6 waypoints now pass the check. NOT WORKING: nav 0/6, and the cause is MM1's yaw defect arriving as an operational blocker -- Nav2 commands its Spin recovery (480 msgs, vx<=0, wz mean +0.213) and this policy cannot rotate in place, so recovery never completes; a costmap flood fill shows every goal cell free but the robot sealed in a 5.5 m2 pocket. Driving the tour manually jammed the robot against the table at (2.52,-1.12) permanently: escape needs in-place rotation (0% of command) or reverse (35-70% error), so the twin cannot recover from obstacle contact at all. MM2's nav gate is blocked behind the MM1b retrain, not behind the world. The door exposes a hinge DOF after being rebuilt as a fixed-base articulation but is kinematically locked -- 500 N.m of direct joint effort moves it 0.000 deg. Six attempts logged; remaining hypothesis is non-uniform scale on articulation links, which PhysX does not support, fix is Xform links with unscaled child geometry as the G1's own USD does. /scan 58.4% vs a 60% gate, cause is C-24 (the G1 twin bridge never spawns cloud_accumulator, so /scan is a single Mid-360 frame) and proven pose-independent. STILL NO VIDEO for MM1 or MM2. Three decisions surfaced to Mohammed: start MM1b retrain (now the critical path for MM2 nav and MM5), authorize the C-24 accumulator change, and whether to spend ~1 h on the door scale hypothesis before MM3.

2026-08-19 | MM2 | world built and verified; nav and door blocked | PARTIAL | panthera_lab is authored by tools/build_lab_world.py and passes 59/59 read-back checks: 8.000 x 6.000 x 2.700 m clear interior, walls AND ceiling (the Mid-360 sweeps a sphere), door articulation with a 35 kg leaf and 0-110 deg limits, table/counter/shelf, six YCB objects at true scale with explicit masses and friction, seeded placement recorded in the layer. Ferox venue with six waypoints, machine-checked against the built world by tools/check_venue_waypoints.py -- which immediately found `home` inside the shelf footprint and `door_outside` standing on no floor at all. SLAM map built and saved. Audit in the new world: 64 pass, 6 Class-A, and all six Class-A are the camera topics under TWIN_CAMERA=0, so no non-camera regression. Six defects were caught by reading artefacts back rather than by reading code, including two where MY OWN measurement was the bug: world-aligned boxes of randomly-yawed objects made every YCB asset look 30-90 mm oversized, which would have been reported as a defect in NVIDIA's assets. BLOCKED: (a) C-25, the door exposes a joint DOF and will not move under 500 N.m of direct effort -- a kinematic lock, six attempts, stopped per section 5; (b) C-26, MoveToNamed reached 0 of 6 waypoints because Nav2's only recovery is Spin and MM1 measured this policy's in-place yaw at 0.0000 rad/s, so it can never recover, and the robot jams on furniture with no way out because escaping needs in-place rotation or reverse and it has neither. C-26 puts the MM1b retrain on the critical path for MM2 and everything after it. C-24 also opened: /scan is a single Mid-360 frame, 58.4% finite against a 60% gate, proven pose-independent.

2026-08-19 | MM1b | retrain configured and root-caused; training DIVERGES on the twin asset | BLOCKED | THE YAW CAUSE IS FOUND, and it is two lines of upstream config, not physics. (1) unitree_rl_lab's limit_ranges.ang_vel_z is (-0.2, 0.2) -- the yaw envelope was capped at 0.2 rad/s, so MM1's measurements at +-0.3/0.6/1.0 were all outside anything the policy ever saw. (2) Worse: CurriculumCfg registers only lin_vel_cmd_levels, which expands x and y and never touches ang_vel_z; the sibling ang_vel_cmd_levels sits in the same module and is referenced from NOWHERE in the repo -- dead code. So yaw stayed pinned at its initial (-0.1, 0.1) for the whole of the original training. The policy was never asked to turn faster than 0.1 rad/s. MM1 spent six hours eliminating obs layout, clamps, hands, friction, restitution, armature and Nav2 before this config was read closely enough. isaac/training/mm1b_g1_omni_v2.py fixes both, widens to Mohammed's envelope, and adds a sampler weighted 25/15/10% to in-place rotation, lateral and reverse. Pre-flight verified: 29 actions, 480 obs, twin USD, dex5 group present, curriculum registered -- and that pre-flight already earned itself, because spawning the 69-DoF twin with upstream's joint_names=".*" silently gave 1080 obs and 69 actions, an undeployable policy that would only have surfaced at export after hours of GPU time. BLOCKER: training diverges. Value-function loss 1.8e10 from iteration ONE, mean episode length pinned at 1.00, and the reward tells you why -- base_linear_velocity -963 and base_angular_velocity -2125 against every other term at ~0.01, i.e. base |vz| ~22 m/s and |w| ~206 rad/s. The robots explode under random actions. Eliminated: PhysX contact-patch overflow (1375 errors, fixed by raising gpu_max_rigid_patch_count to 2^21, now 0-1); env count (512 is no better than 2048); and the env itself (at 64 envs with zero actions the robot spawns at 0.800 upright, settles gently, zero terminations). CONFIRMED CONTRIBUTOR: the Dex5 hands. Damping them (stiffness 20->50, damping 2->10, armature 0.001->0.01) improved base_angular_velocity 8.5x (-2125 -> -251) and mean reward 10x (-32672 -> -3119) -- but it still diverges. 40 light, stiffly-coupled links at a 200 Hz step is a stiff-ODE blow-up and the remaining instability is very likely the rest of that. A smoke mode (--smoke N, checks mean episode length) is now in the trainer so a broken run costs 2 minutes instead of the 47 the first one did. Also this session: C-24 WITHDRAWN -- I claimed the twin's 58.4% /scan fill came from a missing cloud_accumulator, and the real G1 driver has none either (its chain is /livox/lidar -> cloud_restamper -> p2l -> /scan); the accumulator is a Go2 component. The authorized change would have made the twin diverge from the robot. The measurements are real and belong to C-6, now quantified. And the door: scale hypothesis DISPROVEN -- links rebuilt as Xform bodies with unscaled Mesh geometry, DOF still reported, still exactly 0.00 deg under 500 N.m. Parked per the 1 h box.

2026-08-19 | MM1b + film.py | hands locked as specified; divergence bisected; film root-caused | BLOCKED, both root-caused | HANDS LOCKED per Mohammed: tools/build_locked_hands_usd.py converts the 40 finger joints to FIXED while keeping every link, its geometry, mass and inertia -- nothing deleted, nothing lumped by hand, so mass is preserved BIT FOR BIT at 35.004757 kg (0.0000% against the 1% gate) and the finger dynamics vanish. Read back: 0 active revolute hand joints, 29 remain, 49 fixed joints. Then the bisect: contact-patch overflow (1375 PhysX errors) fixed and not the cause; env count not the cause (512 no better than 2048); the env itself is healthy (64 envs, zero actions -> spawns upright at 0.800, settles, ZERO terminations); the hands did NOT cure it when locked, so per the rule they were a symptom; dt 200->500 Hz made it FIVE TIMES WORSE, which rules out a stiff-ODE problem; PD gains resolve to exactly the training values on all 29 joints. The control settled it -- same asset, same physics, only the commands reverted to upstream, and base_linear_velocity went -4031 to -18.3. The divergence is MINE. Bisecting my three changes: widened ranges dominate (-32 with them off), the weighted sampler is second (-139), and removing the yaw curriculum while keeping the rest blows up to 1e22. Launched with yaw widened to +-1.0, linear axes at upstream, curriculum registered, sampler OFF -- DECLARED DEVIATION from the brief's all-three-at-once, because that configuration diverges and a policy trained to nothing delivers neither yaw nor lateral. That run reached iteration 564 and diverged anyway (mean reward frozen at -204303), so the 120-iteration smoke is not a sufficient gate; killed rather than burn 80 minutes. Also learned: "Mean episode length" reads 1.00 in EVERY configuration including the vanilla control, so it is not a usable health signal on rsl-rl 3.1.2 -- the base-velocity penalties are, by three orders of magnitude. FILM.PY ROOT-CAUSED: added a gain read-back to the policy drive and it names the cause -- kp[max]=35809.9 with kd[min]=0.000, which is neither deploy.yaml (tops out near 150, no zero damping) nor the USD (625/0.0). G1VelocityPolicy.initialize() never applies the deployed gains inside film.py, so the robot in every clip was never driven by the controller the clip was meant to show. Three real bugs fixed along the way (paused timeline, spawn height, 1/60 physics step), three eliminated, one reverted for making it worse. STILL NO CLIP: the honest route is to film the live sim, where this same policy walks at 2.4% error.

2026-08-19 | MM1b | reference baseline established; hand COLLIDERS are the cause; my earlier root cause WITHDRAWN | MAJOR PROGRESS | First, a correction that had to come before anything else: I claimed the deployed policy "was never asked to turn faster than 0.1 rad/s" because upstream caps limit_ranges.ang_vel_z at (-0.2,0.2) and its ang_vel_cmd_levels curriculum is dead code, and I reported that as two lines of config rather than physics. The checkpoint's OWN env.yaml says ranges.ang_vel_z = (-1.0, 1.0) and limit_ranges.ang_vel_z = (-1.0, 1.0), and PROVENANCE_g1_omni.md states the widened envelope in words. The training DID sample yaw across +-1.0 from iteration zero; the patch pinned yaw straight to the limit, which is why the missing curriculum never mattered. MM1b's premise was unsupported. I had read upstream's velocity_env_cfg.py and never opened the checkpoint's own params/env.yaml. Then the reference runs, all on the checkpoint's recipe, only the asset differing: (a) upstream G1 -> mean reward +3.96, base_linear_velocity -0.0017, episode length climbing 13 -> 100 -> 181, bad_orientation 2/1024. THE INSTALL IS FINE. (b) twin with hands locked -> reward -359324, base_lin -10926, episode length 1.00, bad_orientation 236/1024. The asset causes it, and fixing the joints was not enough because a fixed joint removes a DOF, not a collider. (b2) same plus the 40 hand `collisions` subtrees deactivated -> base_lin -0.0054, base_ang -0.0116, reward -1.29, mass still exactly 35.004757 kg. THE HAND COLLIDERS ARE THE CAUSE -- two million times better on the linear term, into the reference's own regime. BUT (b2) does not learn: episode length pinned flat at 9.05 across 300 iterations while the reference climbs past 180. Stable is not healthy and the gate is the trend. Episode length 1.00 is now explained rather than waved away: the termination manager shows bad_orientation firing on 236 of 1024 envs in (b) against 2 in (a), so episodes are one step long because the robots are destroyed on contact. Remaining gap is hand mass and inertia at the end of the arms against reward shaping tuned for the bare robot. Next per plan: g1_29dof_train.usd with ONE rigid link per hand carrying total mass/CoM/inertia and a convex-hull collider -- which also restores a collider, since (b2)'s hands pass through the world.

2026-08-19 | MM1b + media + MM3 | training healthy after a 9 kN spawn penetration was found; both media routes blocked by C-23; MM3 surveyed | MIXED | RETRAIN: the one allowed knob -- the spawn-pose collision check -- found the real blocker. Contact forces at reset: upstream reference 0 bodies in contact; twin 8794 N between left_wrist_pitch_link and base_link00L, 7909 N on the right. Each palm collider interpenetrates the wrist it hangs from, so the solver began every episode resolving a 9 kN penetration. It also explains the one thing I could not account for: b2 and the palm-collider variant plateaued at the IDENTICAL 9.05 because the palms are body0-only in the joint graph, were never in hand_link_paths, and kept their colliders in BOTH -- the two assets differed in name and in nothing physics could feel. Builder now disables hand ROOT links too, found from the joint graph: 42 subtrees, mass still exactly 35.004757 kg, spawn contacts 0 -- identical to the reference. Rerun: episode length 12.0 -> 13.6 -> 15.7 -> 17.8 -> 19.6 -> 21.5 monotonic, bad_orientation 109 -> 44, reward -1.29 -> -0.13. Not >100 by 300 like the bare robot, but a different regime from the flat 9.05, and the twin carries 1.5 kg of hand mass at the wrists the bare G1 does not. Trending, so it lives; (c) launched in the background with the in-place/lateral weighted sampler, 6000 iters. MEDIA: built isaac/twin/film_live.py, an offscreen follow camera on the RUNNING sim -- the one that walks -- flag-gated on TWIN_FILM=1, to avoid film.py's standalone scene where the policy's gains are never applied. Clean A/B: TWIN_FILM=0 boots to the main loop at 40 s with odom publishing; TWIN_FILM=1 prints "[FILM] live camera up" and SIGSEGVs with a core dump, 0 frames. That is C-23, already declared, and it means BOTH media routes are blocked on a 16 GB box for different reasons -- live sim segfaults on camera creation, standalone cannot drive the policy. Neither is a bug left to fix. MM1 and MM2 are PASS-with-deviations for media and say so. MM3: prerequisites surveyed -- the bridge, unitree_sdk2py and the unitree_hg IDL are all absent and this gate builds/bakes them. One conflict surfaced BEFORE writing code: MM3 (c) asks for rt/lowstate at 500 Hz +-2%, and the driver's own evidence measures the real robot at 851.4 Hz with 3.6% duplicated ticks carrying DIFFERENT accel values. A bridge built to 500 Hz passes the stated gate and is wrong against the robot. Same class of error as C-24. Recorded in evidence/MM3/PREREQS.md rather than resolved by quietly picking one.

2026-08-19 23:5x UTC | MM3 | PREREQS answered: lowstate rate MEASURED, bridge image built | PASS | The 500 Hz in the gate was a conflation of two different rates and neither is 500 Hz-as-published. Measured off the DT bag (`scripts/mm3_lowstate_rate.py`): `/lowstate` = **1041.677 Hz**, period 0.9600 ms, per-second counts 1039..1044 (sigma 0.79 over 34 full seconds) -- a hardware clock, not a software timer. Gate (c) is therefore rewritten to "matches 1041.68 Hz +-2% (1020.84..1062.51) with the same field semantics"; 500 Hz survives as `pd_apply_hz`, gated separately. Both are now in `isaac/twin/g1_contract.yaml` under a new `lowlevel:` block with provenance, plus the field semantics. Built `ferox/twin-lowlevel:humble` (`docker/Dockerfile.lowlevel`, 1.24 GB): ROS `unitree_hg` from unitree_ros2 + `unitree_sdk2py` + cyclonedds 0.10.2 built from source + rosbag2 -- the first image on this box that can read the DT bag's `/lowstate`. Two build bugs worth keeping: `rosidl_generator_dds_idl` is absent from `ros-humble-ros-base` and unitree_hg needs it; and overlays put in `/etc/bash.bashrc` are invisible to non-interactive shells, so `unitree_hg` imported in an interactive shell and vanished in `docker run img python3 -c ...` while pip-installed `unitree_sdk2py` worked either way -- moved to an ENTRYPOINT.

CORRECTION to the survey commit (faa8cea): it recorded `g1_twin_gt` as "the twin's own ground-truth bag". It is not -- it is a **real-robot** capture (contract `gt_bag`: "G1 #1 standing still, driver + livox sidecar up"), and it must be, because it carries `unitree_hg/msg/LowState` and until this image existed nothing on this box could produce that type. This matters: it is the entire basis for using the bag as the reference for test (c).

The 851 vs 1041 discrepancy is RESOLVED, and not the way I first guessed. Decoding the bag's own `tick` field (`scripts/mm3_lowstate_decode.py`): tick advances at **1000.064 /s**, i.e. it is a millisecond counter 0.006% off nominal 1 kHz. The robot publishes 1041.68 msg/s against that 1000 Hz tick, so 4.02% of messages repeat a tick while carrying *fresh* IMU data (1158 duplicate pairs disagree on accel) -- matching the driver probe's independently measured 3.6%. The bag loses 10 ticks out of 34559 (0.03%), so it is NOT a lossy capture; the driver's 851.4 Hz capture is, its tick clock running 7.7% slow (923.3/s) against a robot that ticks at 1000.064/s. My first written hypothesis -- that 851 was downstream loss from a 1041.7 Hz source, on the strength of `/secondary_imu` also reading 1041.7 -- was wrong in mechanism and the coincidence carries no weight. Prefer the bag, now on evidence rather than instruction.

Field-parity reference extracted from all 35998 messages, now contract: `motor_state` is always 35 long but only slots **0..28 are driven** (29-DoF G1) with `mode=1`; slots 29..34 are identically zero in every message; **`ddq` is never populated** by the robot; `version=[0,0]`, `mode_pr=0`, `mode_machine=5`, `wireless_remote` and `reserve` all zero; `imu_state.rpy` IS sent (not derived by the consumer); quaternion is w-first; accel +z ~ +9.80 upright. The twin will be gated on matching *these*, not on filling every field the IDL has.

Dex3 wire: the DT campaign's 20-entry Dex5 wire is legal -- both the DDS IDL (`types.sequence`) and the ROS msg (`MotorCmd[]`) are unbounded. Only `unitree_sdk2py.idl.default`'s factory is Dex3-shaped and pre-fills 7, so the bridge must not use it. Recorded in the contract.

Retrain (c) confirmed alive and NOT a child of any chat shell -- it runs in docker container `mm1b_c` (parent containerd-shim, PID 1), log persisted on the host at `ferox-g1-locomotion/mm1b_logs/mm1b_c.log`. At ~510/6000 iterations: mean reward -3.35 -> -0.02, episode length 11 -> 29 steps, both monotonic. 50 Hz control against 20 s episodes means the 1000-step ceiling is far off and everything still terminates on `bad_orientation`; that is expected at 8.5% of the run. Left running, ETA ~5 h; re-check at the next gate boundary.

2026-08-20 01:0x UTC | MM3 | low-level DDS bridge built, five of six gates pass, test (a) is not satisfiable by the ROBOT either | PASS-with-deviations | Full write-up in `docs/mm/RESULTS_MM3.md`. `ferox/twin-lowlevel:humble` + `isaac/twin/lowlevel_bridge/` (two processes: `sim_side.py` inside Isaac sampling state and running PD, `dds_side.py` pacing `rt/lowstate` at the measured rate, talking over a seqlock in shared memory -- a 1 kHz publisher cannot be driven off a physics callback, and this keeps cyclonedds out of Isaac's python). (b) fail-closed 6/6 inside the deadline at 85.3-98.6 ms. (c) 1040.752 Hz in the bag's +-2% band with 16/16 field-semantics checks. (d) no ROS topic degraded and two IMPROVED. (f) 20-entry HandState_ at 208.3 Hz with fingers driven and passives not. (e) declared.

TEST (a) IS THE FINDING. Free-standing, the twin topples in ~0.7 s while its joints track their targets perfectly (knee 0.344/0.322/0.309/0.311 against a 0.300 target as base_z goes 0.7975 -> 0.094). It falls over like a statue. Controlled against Isaac's OWN implicit drive, handed the locomotion policy's own deploy.yaml gains with this bridge's torques disabled entirely: it topples IDENTICALLY, base_z 0.095 and pitch 1.471 by t=2-4 s. So the explicit 500 Hz PD is not too soft -- a joint-space PD does not balance a humanoid, whichever code evaluates it, and the sdk2py example's own zero posture fails too (backward instead of forward). The real G1 does not stand from bare low-level PD either: its stance comes from the built-in motion mode, which is exactly why `g1_low_level_example` opens by calling `MotionSwitcherClient.ReleaseMode()` to take control AWAY from it. Test (a) as written is not satisfiable by the robot. Same class as the 500 Hz. On the declared rig (C-30, base pinned -- how the example is run on hardware, with the robot hanging) it holds the commanded stand a full 60 s at 0.042 rad mean tracking. Standing under lowcmd is now an explicit dependency on MM4, which CAMPAIGN 4.4 already assumed.

Two of the gate's own numbers were wrong against the robot. 500 Hz was two rates conflated: measured 1041.677 Hz publication, 500 Hz PD, now separate contract entries. And 100 ms is a DEADLINE, not a threshold -- a watchdog whose threshold IS 100 ms can only fire after it, and the first measurement was 106.47 ms. At 95 ms it still missed one in three (102.71) because the watchdog was evaluated on the PD decimation, giving it pd_hz x RTF ~ 3.9 ms of wall resolution with Isaac's render stalls on top. It now runs every physics step with a 15 ms jitter budget.

THE 851 vs 1041 QUESTION IS SETTLED and my earlier hypothesis was wrong in mechanism: tick is a MILLISECOND counter at 1000.064/s, the robot publishes 1041.68 msg/s against it and genuinely repeats ~4% (driver saw 3.6% independently), the bag loses 10 ticks in 34559, and the DRIVER capture is the lossy one at 7.7% slow. The `/secondary_imu` coincidence carries no weight. Prefer the bag, on evidence.

PHYSICS RATE IS A WIRE DECISION, not a performance knob. The twin's publishers decimate an INTEGER number of steps off the physics clock, so physics_dt decides which contract rates are reachable at all -- the same arithmetic as C-15/C-16 on the Go2. At the 500 Hz 4.2 specifies, three interfaces alias off contract (/livox/imu 500/3=166.67, imu/data 500/6=83.33, /tf 500/10=50.00). At 1000 Hz every rate lands, /livox/imu EXACTLY 200. I had switched to 500 Hz on the strength of a wall-clock `ros2 topic hz` reading -- which is the wrong instrument for this twin, since DT2 gates its sensors in SIM time and every topic read low by the same factor, i.e. the real-time factor, not a broken publisher. Corrected by measuring stamps. Cost is RTF 0.44-0.52 (C-29).

FOUR BUGS, all one family -- state that looks live and is not. (1) Split shm ownership: whoever attaches before the other re-creates a segment keeps a mapping to an UNLINKED one and reads frozen data forever, silently; the sim latched a dead cmd_count=31500 from the previous run, concluded it had been commanded, and sat in fail-closed while the robot folded -- cost a full round of failed stand tests. (2) A torn seqlock read read as command loss, flapping fail-closed every 50-300 ms through a stand that was being commanded throughout (5628 torn reads in 45 s). (3) Two `apply_action` calls in one physics step: ArticulationController keeps ONE pending action, so the body's effort action silently replaced the hand's position action -- 8927 applications counted while Yaw_11L sat pinned at 0.6802, its URDF upper limit. (4) `set_joint_efforts` reports success and does nothing; the idle hold saturated a 139 Nm knee against a 1.3 rad error and the robot still folded, which is not what a saturated actuator looks like.

Also: my own experimental hygiene failed once and cost two boots -- I changed control mode, hand variant and world in a single step, then blamed the bridge for a SIGSEGV that turned out to be C-23's camera crash (`TWIN_CAMERA` defaults to 1 in the launcher). The policy-mode control run is what cleared the bridge.

MEDIA: none, deferred to the 4090 day per C-23, stated per gate in RESULTS_MM3. The camera crash was reconfirmed this gate.

RETRAIN (c) at the MM3 boundary: alive, ~3080/6000 iterations, 2h03m elapsed. Mean reward -3.35 -> +0.88, episode length 11 -> 45.7, both still monotonic. Per the documented criterion ("the gate is the trend") it lives. Flagging honestly that 45.7 steps is 0.91 s against a 1000-step ceiling, and the upstream reference reached 181 by iteration 300 -- so (c) is very unlikely to match the reference by 6000. The twin carries 1.5 kg of hand mass at the wrists the bare G1 does not.

2026-08-20 02:5x UTC | MM4 | SONIC x86_64 image built and RUNNING, closed-loop on the twin; scripted sequence not done | PARTIAL | Full write-up in `docs/mm/RESULTS_MM4.md`. Two of the three things MM4 is for landed. (1) `ferox/sonic-deploy:v1.1-x86_64` (4.8 GB) built from `docker/sonic-deploy/Dockerfile.x86`, a port of the W3 aarch64 recipe whose own build.sh refuses to run off aarch64. Seven deltas, each marked X86 DELTA in the file: just/ORT x86 tarballs, the TensorRT wheel resolving to manylinux_2_28_x86_64 (3.71 GB against aarch64's 2.5), IS_THOR deliberately NOT set (the -lcudla branch it guards is aarch64-only, so setting it would assert something false about the hardware), the arch assertion inverted, vendored DDS libs from thirdparty/lib/x86_64, and TensorRT kept at W3's 10.15.1.29 rather than the campaign's 10.13 -- W3's own comments record 10.13 as the version that came out internally inconsistent, headers 10.13.0.35 against libs 10.13.3.9. Sources: upstream pinned at 54d0b102 with LFS x86_64 libs pulled, artifacts from the PUBLIC nvidia/GEAR-SONIC (encoder 50 MB, decoder 150 MB, planner 774 MB), no token used.

(2) THE LOOP CLOSES. SONIC reads the twin's `rt/lowstate` and prints "G1 type: 5" -- that 5 is `mode_machine`, the value MM3 measured in the DT bag and put in the contract, so the field-parity work is precisely what makes SONIC accept the twin at all. It then commands `rt/lowcmd` at 499.31 Hz and `rt/dex3/{left,right}/cmd` at ~500 Hz, and the twin follows (mode=cmd, knee_L 0.300 -> 0.670, hip_p_L -0.100 -> -0.312). AND `--disable-crc-check` IS NOT NEEDED: W3's compose passes that flag for sim with the comment "the MuJoCo bridge does not produce the CRC the real robot does" -- the MM3 bridge does, crc_bad=0 across 22469 commands with checking left ON. The twin is closer to the robot than the reference sim on exactly the axis MM3 gated. The MM3 watchdog also fired for real: when SONIC stopped commanding, the twin went FAILCLOSED and stayed there.

(3) NOT DONE: the stand -> weight shift -> walking -> turn -> stop -> POSE sequence. SONIC's ZMQ planner handshake never completes -- "Planner Init timing - Model: 5494us" (so the message parses and inference RUNS) then fourteen "Waiting for planner to be initialized" and a timeout. Two bugs found and fixed getting that far, both of which presented as silence rather than error: `dtype "b8"` is not in SONIC's vocabulary (it compares against exactly bool/u8/i8/i16/i32/i64/f16/f32/f64), which made every command message fail the start/stop/planner field check while SONIC held a static default pose looking perfectly healthy; and `movement`/`facing` are 3-vectors, not 2, because OnPlannerReceived memcpy's a fixed i<3 loop out of each buffer -- a 2-element field is read one float past its end, which is not a parse error, just a garbage third component. A third mismatch remains unidentified. OQ 2 asks whether a known-good zmq_manager publisher exists on the Spark; writing one from the headers got two thirds of the way.

DDS DISCOVERY, worth remembering: SONIC takes its interface as argv[1] and is given `lo`. A bridge left on the cyclonedds default never discovers it, and the symptom is SONIC printing "LowState is not available, waiting for robot to be ready" forever while the bridge happily publishes 1041 Hz to nobody. `--iface lo` on both sides.

RIG: C-30 gains an `until_commanded` mode. SONIC needs ~15 s to build/load its TensorRT engines and MM3 established the twin cannot stand unaided for those 15 s, so without it SONIC always inherits a robot already face-down and the test measures the bring-up race rather than the controller. Hold the base until a controller has had authority for N s, then release -- which is how a real G1 is brought up anyway, on a hoist and lowered once the controller is live. The threshold is a number I chose, not measured (OQ 3).

MEDIA: none, C-23, per gate.

RETRAIN (c) at the MM4 boundary: alive, 4310/6000 iterations, 2h43m. Reward -3.35 -> +1.02, episode length 24 -> 50.5, monotonic throughout (601:0.09/30.8, 1801:0.52/39.2, 3001:0.86/45.5, 4201:1.01/50.4). Trend holds so it lives, but the honest read is unchanged and now firmer: 50.5 steps is 1.0 s against a 1000-step ceiling, the upstream reference reached 181 by iteration 300, and linear extrapolation to 6000 gives ~60. This run will not produce a walking policy. It is worth finishing because the trend is the documented gate and the curve is clean, but MM1b should be planned as "the twin's hand mass needs reward re-shaping", not as "one more overnight".

MM5 NOT STARTED. It depends on MM4's sequence for the SONIC-standing hand-off, and MM4 stopped short of that.

2026-08-20 0x:xx UTC | MM3 / MM4 / MM1b | (a1) green so MM3 is PASS; SONIC handshake CLOSED; retrain finished and parked | MM3 PASS, MM4 PARTIAL | Per Mohammed's four decisions.

(1) MM3 IS PASS. Test (a) rewritten as (a1) suspended-base PD hold + (a2) balance-under-lowcmd moved to MM4. (a1): 29 joints held over rt/lowcmd for 60 s at 499.8 Hz, all-joint mean error 0.00972 rad, 28 of 29 inside the 0.05 gate. The 29th separates cleanly and is NOT the bridge -- left_ankle_roll and right_ankle_roll receive the same 7.04/7.11 Nm from the same command through the same code and settle 0.13 rad apart; mirrored code cannot fail one-sidedly, mirrored mechanics can (C-34). Two gain findings, both measured before tuning rather than after: the wrists are gain-limited and not effort-limited (torques checked against the URDF clamp first, nothing saturates -- the twin simply carries a 1 kg hand that upstream's bare-G1 gains never sized for), and damping is not a free parameter at 500 Hz (kd unscaled leaves a free-hanging ankle limit-cycling at +-30 rad/s while holding position to 0.014 rad, which REPORTS as 0.057 rad of steady error that no extra kp removes; kd x gain fails 17 joints outright). C-35 records that the specified 500 Hz PD is marginal on the lightest links: at 1000 Hz the same test gives 0.005 rad all-joint mean against 0.021.

(2) TensorRT 10.15.1.29 is the campaign pin with W3 provenance; C-32 withdrawn as a decision rather than a deviation.

(3) THE SONIC HANDSHAKE IS CLOSED, and the answer was in the repo exactly as Mohammed said. `panthera-g1-wbc tools/scripted_walk.py` is the driver that closed this on the Spark, and it IMPORTS NVIDIA's own builders from `gear_sonic.utils.teleop.zmq.zmq_planner_sender` instead of reimplementing them; this gate now does the same. Reverse-engineering had three further errors, all silent: WALK is 2 not 1 (IDLE/SLOW_WALK/WALK/RUN), speed/height use -1.0 for "mode default" so 0.0 is a literal zero speed, and the start command must be re-sent every beat because ZMQ PUB drops anything published before a SUB connects. BUT NONE OF THOSE WAS THE BLOCKER. SONIC subscribes to `rt/secondary_imu` -- the torso IMU, robot_parameters.hpp:28 -- and refuses to enter its control loop without it; it is not part of LowState_, so a twin publishing a perfect rt/lowstate still looks like a robot with no torso IMU. The bridge now publishes it at 1041.68 Hz composed through the waist chain (C-36). The irony is recorded: MM3 measured /secondary_imu at 1041.7 Hz in the driver evidence and I dismissed it as a rate coincidence. It was not about rates -- it was the name of a topic the twin needed. Result: "G1 type: 5" -> "Planner initialized successfully!" and all 14 sequence segments delivered.

MM4 REMAINS PARTIAL on one thing: SONIC will not balance the twin free-standing. Every run ends on SONIC's own guard, `body_dq[24] = 45.32 > 35`. Four causes found and fixed en route and it still trips: the rig LIES to a balance controller (pinned base means "upright, motionless" whatever it commands, so it drove the knee to 1.011 and the twin was dropped out of a crouch -- the rig phase is now a stance-holding pre-charge that ignores lowcmd); the twin's USD ships NO joint armature so an explicit PD acts on bare link inertia (TWIN_ARMATURE=0.01, the twin's own training value, now on 69 joints -- and the decisive check first: with the policy driving and the bridge passive |dq|max is 0.899 rad/s, so Isaac's velocities are clean and the chatter is mine); `handoff` never got the 1000 Hz physics override because it was gated on == "lowcmd", so every hand-off run silently ran PD at 200 Hz, the exact regime C-35 called unstable; and the hand-over was a step change from the policy's kp~35810 implicit drive to SONIC's kp~100, now ramped (bumpless transfer). After the armature and physics fixes SONIC stays alive in mode=cmd with sat=0/29 and modest torques -- steady-state chatter is gone -- but the twin still goes down within seconds of hand-over. Next: hand over at the instant SONIC's planner initialises rather than N seconds after lowcmd goes live; verify IMU sign/frame against the bag; check SONIC's expected joint ORDER (it indexes isaaclab_to_mujoco internally and body_dq[24] is the recurring offender).

NEW: G1_CONTROL=handoff. The omni policy holds the robot standing while SONIC boots (it needs 20-60 s and MM3 (a2) established the twin cannot stand unaided for one second), then the bridge takes the articulation ONCE and the policy is never stepped again. One-way and latched, so SONIC and omni still never co-drive. This is the pattern CAMPAIGN 4.4 already specifies for MM5. Two bugs in it worth keeping: a single torn seqlock read cancelled the countdown forever (1 kHz reader against a 500 Hz writer, isolated torn reads are normal -- now only a SUSTAINED loss cancels), and `_cmd_fresh()` cached its first snapshot and never re-read, so its stamp aged past the timeout and it returned False for the rest of the run; the active path hid this because _apply_pd refreshed the cache every cycle, and PASSIVE mode did not.

(4) RETRAIN (c) FINISHED 6000/6000 in 3 h 27 min (147 456 000 timesteps) and is PARKED. `ferox-g1-locomotion policy/PROVENANCE_v2_mm1b_c.md` has the curve and the conclusion: reward -3.35 -> +1.24, episode length 24 -> 55.03, monotonic, no divergence -- and not a walking policy. The ceiling is 1000 steps; every episode still ends in bad_orientation and none ever times out, so it survives ~1.1 s. The upstream bare-G1 reference on the identical recipe passed 181 steps by iteration 300. It learned to fall over more slowly. That is reward shaping against 1.5 kg of Dex5 hand mass at the end of the longest lever on the robot, not compute, and the document exists to retire "one more overnight". SONIC is the turning controller of record; MM4's stand/walk/turn is the yaw demo.

MEDIA: none, C-23, per gate.

2026-08-20 06:xx UTC | MM3 rewritten + MM4 + retrain closed | MM3 PASS, MM4 PARTIAL (one item open), retrain parked | Decisions 1-4 all actioned.

(1) MM3 -> PASS. Test (a) split per Mohammed into (a1) suspended-base PD hold and (a2) balance-under-lowcmd, the latter moved to MM4. (a1) holds 29 joints over rt/lowcmd for 60 s at 499.8 Hz with an all-joint mean error of 0.0097 rad; 28 of 29 inside the 0.05 gate. The 29th separates cleanly and is NOT the bridge: left_ankle_roll and right_ankle_roll receive the same 7.04 / 7.11 Nm from the same command through the same code and settle 0.13 rad apart -- mirrored code cannot fail one-sidedly, mirrored mechanics can (C-34, OQ 5). Two gain findings on the way, both measured before anything was tuned: the wrists are GAIN-limited not effort-limited (torques checked against the URDF clamp first, nothing saturates -- the twin simply carries a 1 kg hand that upstream's bare-G1 gains never sized for), and damping is not a free parameter at 500 Hz -- kd unscaled leaves a free-hanging ankle limit-cycling at +-30 rad/s while holding POSITION to 0.014 rad, which reports as 0.057 rad of steady "error" that no amount of kp removes, while kd scaled with kp fails 17 joints outright. At 1000 Hz PD the same test gives 0.005 rad against 0.021 (C-35). The (a2) topple finding and the ReleaseMode parallel are recorded in RESULTS_MM3 as the reason for the rewrite -- and SONIC's deploy turns out to literally call MotionSwitcherClient::ReleaseMode() at init, which is that argument in upstream's own source.

(2) TensorRT 10.15.1.29 recorded as the campaign pin with W3 provenance; C-32 withdrawn as a decision rather than carried as a deviation.

(3) THE PUBLISHER WAS IN THE REPO. `panthera-g1-wbc tools/scripted_walk.py` drives exactly this and imports NVIDIA's own builders from `gear_sonic.utils.teleop.zmq.zmq_planner_sender` instead of reimplementing them; this gate now does the same, so the wire format stays owned by upstream. It corrected three silent errors in my hand-rolled version (WALK is 2 not 1; speed/height use -1.0 for "mode default" and my 0.0 was a literal zero speed; the start command must be re-sent every beat because ZMQ PUB drops anything sent before a SUB connects). BUT THE THIRD MISMATCH WAS NOT A FORMAT PROBLEM AT ALL -- it was a missing TOPIC. SONIC subscribes to `rt/secondary_imu`, the G1's torso IMU (robot_parameters.hpp:28), and refuses to enter its control loop without it. It is not part of LowState_, so a twin publishing a field-perfect rt/lowstate at exactly the right rate still looks like a robot with no torso IMU. The bridge now publishes it at 1041.68 Hz, composed from the pelvis IMU through the waist chain (C-36), and the handshake closed on the first try: "Planner initialized successfully!". Worth recording that MM3 saw /secondary_imu at 1041.7 Hz in the driver evidence and dismissed it as a coincidence with /lowstate's rate -- it was not a coincidence, it was naming a topic the twin needed.

MM4 NOW: image built and running on x86_64; SONIC closed-loop on the twin (G1 type: 5 read off rt/lowstate, lowcmd accepted at up to 499 Hz, crc_bad=0 with CRC checking left ON -- W3 passes --disable-crc-check for sim because "the MuJoCo bridge does not produce the CRC the real robot does", and this one does); the full 14-segment sequence commanded end to end with SONIC replanning through it; policy latency measured from SONIC's own instrumentation at ~3.0 ms against a 20 ms period. `G1_CONTROL=handoff` is built and works -- the omni policy holds the twin standing at RTF 1.13 while SONIC boots, then the articulation transfers ONCE, latched and one-way, never co-driving. That is CAMPAIGN 4.4's own MM5 pattern, delivered early.

STILL OPEN, and it is the ping: SONIC does not balance the twin free-standing. The cause is measured and is a stance mismatch, not plumbing -- SONIC's nominal pose is a CROUCH (knee 0.669, hip -0.312) and the omni policy's is nearly straight (knee 0.124, hip -0.052), so hand-over has to cross 0.55 rad on the knee under SONIC's own gains (kp 99 knees / 28.5 ankles, read off the wire). Three attempts: instant hand-over spikes joint velocity and SONIC correctly trips its own safety check (body_dq[20] = 36.95 > 35); a bumpless 0.3-6 s blend stops the trip and SONIC survives the whole sequence but the robot still falls, because blending BREAKS SONIC's closed loop -- it commands one thing while the bridge applies another; and the rig is worse still, because a pinned base LIES to a balance controller (observations read "upright, motionless" whatever it commanded, so it drove the knee to 1.011 rad and the twin was dropped out of a crouch). Likely answer, untested: W3 ran SONIC against a MuJoCo sim that SPAWNS the robot in SONIC's own stance, so no hand-over ever existed. Recommending a second twin spawn pose matching SONIC's nominal stance, starting G1_CONTROL=lowcmd there directly. C-37.

Three bridge bugs that only a real controller could have found: `_cmd_fresh()` cached its first read and never re-read, so its stamp aged out and it returned False forever (the active path refreshed the cache incidentally and hid it; PASSIVE mode did not, so hand-over never fired while SONIC commanded at 500 Hz throughout); a single torn seqlock read cancelled the hand-over countdown, which with a 1 kHz reader against a 500 Hz writer meant it restarted forever; and on_physics_step ran twice on the activation step, double-counting sim_time -- from which rt/lowstate.tick is derived.

(4) RETRAIN (c) FINISHED: 6000/6000 iterations, exit 0, ~5 h. Reward -3.35 -> +1.24, episode length 24 -> 55.03, monotonic throughout and PLATEAUED (last 300 iterations mean 55.20, max 56.46). It is not a walking policy: the ceiling is 1000 steps, `Episode_Termination/time_out` is 0.0000 and `bad_orientation` is 1.0000, so every episode ends in a fall at about 1.1 s and none ever reaches the time limit. The upstream bare-G1 reference passed 181 steps by iteration 300 on the identical recipe. Written up as `ferox-g1-locomotion/policy/PROVENANCE_v2_mm1b_c.md` with the honest conclusion: this is REWARD SHAPING, not compute -- the twin carries 1.5 kg of Dex5 hand mass at the longest lever on the robot and every term that trades against it was weighted for a bare G1. Three assets, one recipe, three regimes; the recipe is not the variable. Retraining is parked and nothing downstream loads these checkpoints. SONIC is the turning controller of record and MM4's stand/walk/turn IS the yaw demo.

MM5 NOT STARTED -- it needs SONIC balancing, which is the open item above.

MM4 ADDENDUM: a fifth cause found and fixed, and three hypotheses closed. The Dex5 finger drives keep the URDF import's stiffness -- measured kp 35809.9 with kd 0 -- so driving them from rt/dex3 slams the fingers and the reaction travels up the arm. That is why SONIC's guard kept naming body_dq[20] and body_dq[24], which are ARM joints and not finger ones. With G1_LL_HAND_KP=60 SONIC stops tripping the guard entirely and stays in mode=cmd through the run (C-38). The twin still goes down ~1.7 s after hand-over. CLOSED by measurement: it is not PD chatter and not noisy Isaac velocities (policy driving, bridge passive, |dq|max 0.899 rad/s), and it is NOT a hand-over that precedes planner init -- timestamped, Init Done 09:16:47.4, "Planner initialized successfully!" 09:16:54.3, hand-over ~09:16:55.4, so the planner was live 1.1 s before it got the robot. Remaining suspects, in order: IMU sign/frame conventions against the DT bag (a balance controller fed wrong projected-gravity falls confidently and silently); SONIC's expected joint order end to end (it remaps isaaclab_to_mujoco internally and the wire order has been verified against the URDF and the sdk2py enum but not against SONIC's own consumption); and a field-by-field diff of the twin's rt/lowstate against what the MuJoCo bridge published on the Spark, where this same SONIC build walked 17.53 m.

MM4 ADDENDUM 2, and where it rests. The stance mismatch is the live suspect and is now C-39: SONIC's nominal stance sits at knee 0.669 while the omni policy holds knee 0.124, so hand-over asks the twin to perform a 0.55 rad squat while balancing. Ramping that transition over 8 s instead of 2 does NOT fix it -- the twin still goes down, so this is not merely a rate-of-change problem. Two robustness fixes came out of the attempt and both are keepers: the Dex5 finger drives ship at kp~35810 with kd 0 from the URDF import, so driving them from rt/dex3 shakes the ARMS and that is why SONIC's guard kept naming body_dq[20]/[24] rather than finger indices (C-38); and SONIC can emit NON-FINITE rt/lowcmd once its state estimate diverges on a fallen robot, which the bridge turned into tau = kp*(NaN - q) and handed straight to PhysX, poisoning the articulation -- now rejected on both halves, and worth remembering that np.clip PROPAGATES NaN rather than removing it (C-40). Also learned the hard way: a stray driver container holding tcp://*:5556 makes the next run's driver die on bind while the OLD driver's stop command kills SONIC, which reads exactly like a fresh failure; the runner now names the driver container so it can be reaped.

2026-08-20 | MM4 (a) | convention diff done inside the 3 h box; conventions MATCH, C-39 parked open | PARTIAL | Artifact: `docs/mm/evidence/MM4/CONVENTION_TABLE.md`, read from the reference MuJoCo bridge source (gear_sonic/utils/mujoco_sim -- the sim side of deploy.sh sim that fed W3's 17.53 m walk) and cross-checked against the DT bag. No Spark needed. Both of Mohammed's prime suspects were checked FIRST and both cleared: rt/secondary_imu is composed from the right link (torso_link, world quaternion, local-frame gyro, matching mj_data.xquat + mj_objectVelocity flg_local=1), and the quaternion is w-first on both sides and in the bag. Joint order is SDK order, independently confirmed because the reference config lists DEFAULT_DOF_ANGLES in exactly that order. THE DECIDING FACT: projected gravity is computed from the QUATERNION alone (g1_deploy_onnx_ref.cpp:1622, quat_rotate(conj(base_quat),{0,0,-1})) and never from an accelerometer; base_accel, body_torso_quat, body_torso_ang_vel and body_torso_accel appear exactly ONCE in the whole deploy, in a call to LogFullState. So the three real mismatches -- my accelerometer is specific force (+9.8 z, matching the ROBOT) where MuJoCo publishes world linear acceleration (~0 standing), my rpy is populated where MuJoCo leaves it zero, and my ddq is zero (matching the robot) where MuJoCo populates it -- are all log-only and cannot explain the fall. rt/secondary_imu turns out to be a LIVENESS requirement rather than an observation, which is exactly why publishing it at all was the unlock and why getting its accelerometer "wrong" costs nothing. C-39 is therefore dynamic, not representational: the reference pins SONIC's nominal stance at hip -0.1 / knee 0.30 / ankle -0.2 with ALL ARMS 0.0, and the twin hands over at knee ~0.11 with elbows at 0.97 and a 1 kg hand on each, so SONIC crouches (commanding knee 0.669, double its own default) and goes over. Blending to SONIC's own DEFAULT_DOF_ANGLES over 4 s does not fix it; nor does capping the Dex5 gains. Parked per the time-box. C-38 DONE: Dex5 finger drives capped to kp 5.0 / kd 0.1 in run.py at articulation init so it holds in every mode -- the URDF import left them at kp 35809.9 with kd 0.000 against a 0.93 N m effort limit, i.e. saturation at 26 microradians, and configs/wbc has no finger gains to copy because it declares them passive (NUM_HAND_MOTORS: 0). Recorded in TWIN_DEVIATIONS.

2026-08-20 | MM5 | pipeline built and run, N=20, 0/20 -- and it is C-39 again from the other side | FAIL (number is the deliverable) | `docs/mm/RESULTS_MM5.md`. Balancer is the OMNI POLICY, stated per CAMPAIGN 4.4 ("or omni standing if MM4 slips -- say which"); the SONIC seam is G1_CONTROL=handoff and MM5 runs behind it unchanged when C-39 closes. 20 trials on soup_can, seeds 20260821..20260840, cheat-attach used on ZERO trials. Result 0/20 with a single-bucket taxonomy: ROBOT_FELL x20, twelve of them a mean 2.93 s into REACH and eight during APPROACH. THE TWIN CANNOT HOLD ITS BALANCE WHILE THE ARM REACHES -- the omni policy commands all 29 body joints and was trained with a joint_deviation_arms term, so an arm driven off its nominal pose is out of distribution, and each Dex5 hand is 1.0 kg at the longest lever on the robot. This is C-39 arrived at from the other direction: MM4 found SONIC crouches and falls when handed the wrong stance, MM5 finds the omni policy falls when the arm leaves its stance. Whichever controller closes first unblocks both gates.

IT IS NOT THE IK, and establishing that cost three silent bugs. (1) target = measured + dq cannot outrun its own tracking error: the arm lags its target by a few tenths of a radian against kp 40 with a kilo on the wrist, so a target pinned one step ahead of a lagging arm stalls the instant kp*error balances gravity -- measured, the palm crept 0.11 m of a 0.70 m reach in 25 s and stopped, and integrating from the previous TARGET fixed it. (2) ArticulationController keeps ONE pending action per step, so the policy's 29-joint action silently replaced every set_joint_position_targets write and the palm sat at z=0.70 for 25 s while the IK computed targets nothing applied -- arm and fingers now go out as a single apply_action AFTER the policy. That is the MM3 lesson repeating verbatim. (3) Reach geometry: the `table` waypoint is 1.10 m from the objects and the arm reaches ~0.65 m, so navigate-then-reach is geometrically impossible; MM5 stands the robot at a reach pose and stages the can on the table's near edge, stated as a scenario choice (C-42, C-43).

C-41, and it is a real asset defect: the lab's YCB objects ship with RigidBodyAPI and MassAPI and NO CollisionAPI, so every object was in free fall from t=0 -- measured -43 m after 3 s, exactly 0.5*g*t^2 -- and any grasp would have closed on an object already through the floor. MM2 verified 59 properties of this world and passed because MM2 never tried to touch one. Fixed at load (convexHull, 6 objects / 6 meshes); NOT in the builder, because the objects arrive as references whose meshes are not composed at build time, and a builder-side fix produced no colliders for exactly that reason and was reverted rather than shipped unverified.

NAV STAGE NOT ATTEMPTED and said so: MoveToNamed is 0/6 in this venue (C-26) because the omni policy's in-place yaw is 0.0000 rad/s, so Nav2's Spin recovery is the one manoeuvre this robot cannot perform. Running it to watch it fail a seventh time would not have produced information. SONIC can turn, so the navigation stage and the balance stage are blocked on the same thing.

2026-08-20 | C-39 fork + MM5 fixed-base + MM9 | fork BOTH branches negative; fixed-base MM5 0/20 with the grasp stack exercised; persistence written | FORK NEGATIVE, MM5 FAIL, MM9 DONE | Per Mohammed's ordered decisions.

(1) FORK, 30 min box. SONIC alone from t=0, rig holding the twin at SONIC's OWN nominal stance (knee 0.30, arms 0.0, base at 0.731), released with SONIC already commanding. BRANCH 1 (transition) NEGATIVE: base_z 0.060, pitch -1.549, and SONIC not even straining -- 6.5 Nm across 29 joints, sat=0/29. BRANCH 2 (mass) NEGATIVE: with the palms zeroed to 0.34 kg/hand -- 0.69 kg/hand lighter than reality, which is the mass the W campaign proved SONIC against -- the result matches the real-mass run to three figures. So there is NO payload margin to report, because the failure does not depend on payload; the bisect (0.34/0.6/0.8/1.0) was moot and not run. WHAT IT ACTUALLY IS: measured while UPRIGHT on the rig, so not post-fall divergence, and with conventions already verified against both the reference MuJoCo bridge and the DT bag -- SONIC's gains are sane (kp 99 legs, kd 0.9-6.3, tau_ff zero) but it asks for a 1.648 rad knee against its own 0.30 nominal, a -0.859 hip against -0.1, and THREE OF 29 TARGETS BEYOND THE URDF LIMITS. That is not a standing posture. C-39 sits between SONIC's observations and its policy output. Evidence `docs/mm/evidence/MM4/C39_FORK.md`, which also names the one cheap experiment that would settle it: run the same fork against the reference MuJoCo sim, since the wire is already proven identical.

(4) DECOUPLED WBC NOT ATTEMPTED -- image not pullable. `nvgear/decoupled_wbc` does not exist; `nvcr.io/nvidian/gr00t_wbc:base` (from the repo's own docker/image_name.txt) returns Access Denied, being an internal NGC org. Per the decision that parks mobile manipulation and makes MM5 the fixed-base result. Worth noting for a future attempt: the SOURCE is already on this box at `ref/upstream/GR00T-WholeBodyControl/decoupled_wbc`, and its command_sender publishes rt/lowcmd with unitree_hg via unitree_sdk2py -- i.e. it speaks the SAME seam the MM3 bridge already serves, so if the weights can be obtained it drops straight on.

(2) FIXED-BASE MM5, N=20: 0/20, taxonomy REACH_TIMEOUT 11 / LIFT_FAILED 9. NINE OF TWENTY reached the pre-grasp and closed the hand on the can, and nine got to LIFT -- the first time in this program the pipeline has gone past the reach at all (W4's hand never reached the can). Limits: workspace, not solver (with the base 0.72 m back the IK converges rock-stable to 354 mm and stops -- the right arm's effective forward reach is ~0.39 m from the pelvis because it is also reaching DOWN from a 1.1 m shoulder; 0.38 m fixed it, 0.32 m put the body inside the table and knocked the can off before the arm arrived); and position-only IK, so palm ORIENTATION at the pre-grasp is uncontrolled, which is the most likely cause of the LIFT_FAILED rows -- the hand closes in the right place in the wrong attitude. Two distinct LIFT_FAILED modes are already visible and should be split next time: `rose -0.015` is a hand that did not grip, `rose -0.765` is a hand that knocked the can off the table.

I REPORTED A WRONG NUMBER TO MYSELF AND CAUGHT IT. The first fixed-base N=20 came back 2/20 and both successes were artefacts: obj_start was captured by re-reading the object right after set_world_pose, which does not take effect until physics steps, so it returned the PREVIOUS trial's end pose -- and when that trial had knocked the can onto the floor, obj_start came back at z=0.032 and the LIFT test passed in a single step (LIFT: 0.005 s) on a can that had never been touched. The runner now passes the commanded placement instead of re-reading, an unstaged object fails as HARNESS_OBJ_NOT_STAGED, and the run was repeated from scratch. Corrected number is 0/20, so the ENTIRE apparent success rate was the bug. Reporting it would have claimed the program's first Dex5 grasp on a stale pose read.

FOUR SOLVER BUGS, all silent, all fixed en route: the palm pose was FROZEN (get_body_poses does not exist on this Articulation and the USD fallback reads ComputeLocalToWorldTransform, which PhysX never writes back -- the IK servoed against a constant z=0.706 for 25 s while the arm was visibly moving); target = measured + dq cannot outrun its own tracking error (palm crept 0.11 m of a 0.70 m reach and stalled); no joint-limit awareness (DLS wound right_shoulder_yaw into its -2.618 stop and held it while the error oscillated 370-435 mm); and no null-space use on a 7-DoF arm against a 6-DoF task. Plus one that was not the solver: at kp 40 the arm lags ~0.1 rad under a kilo of hand, and that lag IS the servo problem -- a small lead cap is consumed by it, a large one closes the loop through it and limit-cycles; the fixed-base variant stiffens the arm to kp 400 and it disappears.

(MM9) PERSISTENCE: RESUME.md section 0 rewritten for the MM campaign and points the next instance at C-39 first; docs/mm/CAMPAIGN.md carries a gate-by-gate status header; CAPTURES.md records that the whole MM campaign has no media and why (C-23, re-confirmed at MM3 where TWIN_CAMERA=1 crashed the sim at world.step(render=True)).

2026-08-20 | C-39 A/B + grasp v2 | THE A/B ANSWERS IT: SONIC stands in the reference MuJoCo sim and falls in the twin | A/B DECISIVE, grasp v2 0/20 | Evidence `docs/mm/evidence/MM4/AB_MUJOCO.md` and `ab/{mujoco,twin}.json`.

(1) A/B, 3 h box. Stood the reference MuJoCo sim up on THIS box -- `ferox/sonic-mujoco:humble`, the sim side of deploy.sh sim -- and ran the identical fork against it: same x86 image, same deploy binary, same driver, same DDS seam, only the simulator differs. MUJOCO: quat w=0.9992 upright, knees live at 1.401/0.975, alive and "Replanning with mode: SLOW_WALK", executing the driver's weight shift. TWIN: base_z 0.060, pitch -1.549, flat. SO THE X86 PORT IS EXONERATED -- it balances a robot today, on this box. Three things were needed to stand the reference up and are worth remembering: the vendored external_dependencies/unitree_sdk2_python (stock sdk2py has no OdoState_), a git lfs pull of gear_sonic/data/robot_model/** (STLs were pointers and pinocchio dies with "Failed to determine STL storage representation"), and NO MUJOCO_GL at all (egl fails on this box, osmesa needs a library that is not installed, and either makes `import OpenGL` fail before the sim starts).

THE DIFF, 5 s each, numerically: lowstate 197.03 vs 1012.20 Hz; mode_machine 0 vs 5; motor mode 0 vs 1; ddq populated (0.832) vs zero; imu accel [0,0,0] vs [0,0,9.807]; imu rpy zeros vs populated; imu temp 0 vs 80; CRC 0 vs valid -- MuJoCo does not compute a CRC AT ALL, which is exactly why W3 passes --disable-crc-check. EVERY field where the two differ in content is one where THE TWIN IS MORE ROBOT-FAITHFUL THAN THE REFERENCE, and SONIC was only ever validated against the less faithful one. That is the shape of what is left. Also confirms the MM4(a) convention table empirically on every point it predicted.

Hypotheses eliminated this pass, each costing a run: spawn heading (forcing identity yaw, quat w=1.0000, changes nothing -- base_z 0.060, pitch -1.545) and publication rate (dropping the twin to the reference's 197 Hz changes nothing). Added to the earlier five: stance, mass, conventions, IDL mismatch, missing-topic. NEXT EXPERIMENT, set up but not finished in the box: G1_LL_MUJOCO_COMPAT=1 makes the bridge lie in exactly MuJoCo's way (mode_machine 0, motor mode 0, accel/rpy/temp zeroed, crc 0). First attempt was invalid -- zeroing CRC without also passing --disable-crc-check means SONIC rejects every message and never commands (lowcmd=0). Run it as `MJC=1 SONICFLAGS=--disable-crc-check`. If the twin then stands, bisect the six fields; if not, the remaining difference is DYNAMICS -- MuJoCo's G1 model against the twin's Isaac articulation -- and the honest next step is comparing actuator limits and inertias rather than the wire. Artifact sha256s are in the evidence for hashing against the Spark's W3 copies, though the same four artifacts drive SONIC correctly in MuJoCo here, which is its own evidence they are intact.

(2) GRASP V2, N=20: 0/20, taxonomy REACH_TIMEOUT 12 / NO_GRIP 7 / DESCEND_TIMEOUT 1. Stage reach 20/20 REACH, 8/20 DESCEND, 7/20 GRASP, 7/20 LIFT -- and ZERO knocked-off rows, where v1's bucket had been hiding them. Changes: 6-DoF IK with the palm orientation constrained, and the approach axis was MEASURED not chosen -- at the pre-grasp the palm's local z lands on world [-0.096,-0.081,-0.992], so this arm's natural posture already presents the palm downward and the grasp it wants is TOP-DOWN; topdown_quat snaps that axis down and leaves roll free (5-DoF, all a symmetric can needs), weighted 0.35 so orientation cannot steal authority from a workspace-limited reach. Effect was immediate: pre-grasp in ~8 s where position-only IK timed out at 35 s. Plus a separate DESCEND stage, and the LIFT bucket split into NO_GRIP vs KNOCKED_OFF as it should have been the first time.

STILL NO GRASP, and the cause is height not tuning: from a fixed base 0.38 m away the palm tops out around z=0.95 with the can's centre at 0.80, so it reaches the pre-grasp 0.13 m above and cannot descend the last few centimetres -- the hand closes too high to wrap the can. Staging on the lab's 0.90 m counter is the right fix and was attempted; it needs its own base placement (approach from -y puts the right arm on the +x side) and that geometry did not tune inside the box, reaching only 577-641 mm. `surface: "counter"` is wired and is the documented next step.
